import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 4 Article: The Memory Hierarchy in Practice","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-4-Article.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-4-Article.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Articles/Module-4-Article.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-4-article-the-memory-hierarchy-in-practice" tabindex="-1">Module 4 Article: The Memory Hierarchy in Practice <a class="header-anchor" href="#module-4-article-the-memory-hierarchy-in-practice" aria-label="Permalink to &quot;Module 4 Article: The Memory Hierarchy in Practice&quot;">​</a></h1><h2 id="the-big-idea" tabindex="-1">The big idea <a class="header-anchor" href="#the-big-idea" aria-label="Permalink to &quot;The big idea&quot;">​</a></h2><p>The processor is far faster than memory. The <strong>memory hierarchy</strong> exists to paper over this gap: a little fast/expensive memory close to the core, and a lot of slow/cheap memory far away. Every program, every instruction, every data access rides this hierarchy. Getting it right is the single biggest performance lever in a system.</p><h2 id="the-latency-gap-in-numbers" tabindex="-1">The latency gap in numbers <a class="header-anchor" href="#the-latency-gap-in-numbers" aria-label="Permalink to &quot;The latency gap in numbers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Level</th><th>Typical size</th><th>Latency</th><th>Bandwidth</th></tr></thead><tbody><tr><td>Registers</td><td>32–256 B</td><td>~1 cycle</td><td>CPU-width bus</td></tr><tr><td>L1 cache</td><td>32–64 KB per way</td><td>~1–2 cycles</td><td>~ several × CPU width/cycle</td></tr><tr><td>L2 cache</td><td>256 KB–1 MB</td><td>~10–20 cycles</td><td></td></tr><tr><td>L3 cache</td><td>2–32 MB</td><td>~30–50 cycles</td><td></td></tr><tr><td>Main RAM (DRAM)</td><td>GBs</td><td>~200–300 cycles</td><td>~ 10–50 GB/s</td></tr><tr><td>SSD (NAND)</td><td>TBs</td><td>~100,000 cycles</td><td>~3–7 GB/s</td></tr><tr><td>HDD</td><td>TBs</td><td>millions of cycles</td><td>~100–200 MB/s</td></tr></tbody></table><p>A single cache miss to DRAM costs hundreds of instructions. This is why cache behaviour dominates performance.</p><h2 id="sram-vs-dram-why-they-behave-differently" tabindex="-1">SRAM vs DRAM: why they behave differently <a class="header-anchor" href="#sram-vs-dram-why-they-behave-differently" aria-label="Permalink to &quot;SRAM vs DRAM: why they behave differently&quot;">​</a></h2><ul><li><strong>SRAM</strong> (cache): a 6-transistor cell holds a value without refresh. Stable and fast, but uses 6 transistors per bit → low density, high cost.</li><li><strong>DRAM</strong> (RAM): a 1-transistor + 1-capacitor cell stores a bit as charge. Much denser and cheaper, but the charge <strong>leaks</strong>, so the memory controller must <strong>refresh</strong> every row ~every 64 ms. Between accesses, a row is kept &quot;open&quot; as a <strong>row buffer</strong> for fast back-to-back access to the same row.</li></ul><h2 id="how-a-cache-works" tabindex="-1">How a cache works <a class="header-anchor" href="#how-a-cache-works" aria-label="Permalink to &quot;How a cache works&quot;">​</a></h2><p>A cache stores <strong>lines</strong> (typically 64 bytes) copied from main memory. On an access:</p><ol><li><strong>Look in the cache.</strong> If the line holding that address is present → <strong>hit</strong> (fast).</li><li>If not → <strong>miss</strong>: copy the whole 64-byte line from the next level.</li><li>If no space: <strong>replace</strong> a victim line (write it back if dirty).</li></ol><p>Three parameters define any cache: <strong>size</strong>, <strong>block size</strong>, and <strong>associativity</strong>.</p><ul><li><strong>Direct-mapped:</strong> each block maps to exactly one slot → cheap tags, but conflict misses abound.</li><li><strong>Set-associative (n-way):</strong> each set has n slots → balanced. Most modern caches.</li><li><strong>Fully associative:</strong> any line anywhere → expensive, low conflict.</li></ul><h2 id="cache-miss-types-the-cache-miss-hit-topics" tabindex="-1">Cache miss types (the &quot;Cache Miss-Hit&quot; topics) <a class="header-anchor" href="#cache-miss-types-the-cache-miss-hit-topics" aria-label="Permalink to &quot;Cache miss types (the &quot;Cache Miss-Hit&quot; topics)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Miss type</th><th>Cause</th><th>Example</th></tr></thead><tbody><tr><td>Compulsory (cold)</td><td>First reference to a block</td><td>First loop iteration over an array</td></tr><tr><td>Capacity</td><td>Working set larger than cache</td><td>Iterating arrays bigger than L2</td></tr><tr><td>Conflict</td><td>Two blocks map to the same set in a set-associative cache</td><td>Two arrays same size, stride = cache size</td></tr><tr><td>Hit-after-miss</td><td>After a miss, the retried access now hits</td><td>Loading a line, then reading adjacent bytes</td></tr></tbody></table><h2 id="writing-back-and-writing-through" tabindex="-1">Writing back and writing through <a class="header-anchor" href="#writing-back-and-writing-through" aria-label="Permalink to &quot;Writing back and writing through&quot;">​</a></h2><ul><li><strong>Write-through:</strong> write to cache <em>and</em> next level immediately. Simple, but high traffic.</li><li><strong>Write-back:</strong> write only to the cache line; write the next level <strong>only when the line is evicted</strong> (and only if a <strong>dirty</strong> bit says it changed). Lower traffic, higher complexity.</li><li><strong>Write-allocate</strong> (fetch the line on a read-for-ownership after a miss) vs <strong>no-write-allocate / write-around</strong>.</li></ul><h2 id="amat-one-formula-to-rule-them-all" tabindex="-1">AMAT: one formula to rule them all <a class="header-anchor" href="#amat-one-formula-to-rule-them-all" aria-label="Permalink to &quot;AMAT: one formula to rule them all&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AMAT = Hit time + Miss rate × Miss penalty</span></span></code></pre></div><p>Worked numbers: if the L1 hit takes 1 cycle, the miss rate is 5%, and fetching a line from L2 costs 20 cycles,</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AMAT = 1 + 0.05 × 20 = 2 cycles average</span></span></code></pre></div><p>Halving the miss rate to 2.5%: <code>1 + 0.025 × 20 = 1.5</code>. Because misses are expensive, small reductions in miss rate have outsized impact.</p><h2 id="cache-coherence-on-chip" tabindex="-1">Cache coherence on chip <a class="header-anchor" href="#cache-coherence-on-chip" aria-label="Permalink to &quot;Cache coherence on chip&quot;">​</a></h2><p>When multiple cores each have caches, the same physical address may exist in several caches. <strong>MESI</strong> (Modified, Exclusive, Shared, Invalid) is the classic protocol:</p><ul><li><strong>Modified:</strong> this cache owns a dirty copy; others are invalidated.</li><li><strong>Exclusive:</strong> clean; only this cache has it.</li><li><strong>Shared:</strong> clean copy; other caches may have it too.</li><li><strong>Invalid:</strong> this line is stale; others may hold it.</li></ul><p>A read miss that finds a line in another cache&#39;s Modified state triggers a <strong>cache-to-cache transfer</strong> (snooping or directory-based).</p><h2 id="exam-angle" tabindex="-1">Exam angle <a class="header-anchor" href="#exam-angle" aria-label="Permalink to &quot;Exam angle&quot;">​</a></h2><p>For a &quot;cache mapping&quot; question:</p><ol><li>Compute the index: <code>block_number mod number_of_sets</code>.</li><li>Compute the tag: <code>block_number / number_of_sets</code> (or <code>block_number div sets</code>).</li><li>Determine hit/miss, noting compulsory vs conflict vs capacity.</li><li>For AMAT, plug into <code>Hit time + Miss rate × Miss penalty</code>.</li></ol><blockquote><p>Tip: remember that a 64-byte line means consecutive addresses often &quot;miss once then hit&quot; — this is <strong>spatial locality</strong>, the reason big lines help.</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Articles/Module-4-Article.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module4Article = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module4Article as default
};
