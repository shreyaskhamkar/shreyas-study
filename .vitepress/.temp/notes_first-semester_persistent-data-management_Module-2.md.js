import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 2: Relational Database Design","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/persistent-data-management/Module-2.md","filePath":"notes/first-semester/persistent-data-management/Module-2.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/persistent-data-management/Module-2.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-2-relational-database-design" tabindex="-1">Module 2: Relational Database Design <a class="header-anchor" href="#module-2-relational-database-design" aria-label="Permalink to &quot;Module 2: Relational Database Design&quot;">​</a></h1><p><a href="./Module-1">← Previous: Module 1</a> · <a href="./README">Subject index</a> · <a href="./Module-3">Next: Module 3 →</a></p><h2 id="learning-outcomes" tabindex="-1">Learning outcomes <a class="header-anchor" href="#learning-outcomes" aria-label="Permalink to &quot;Learning outcomes&quot;">​</a></h2><p>After completing this module, you should be able to:</p><ul><li>explain the central ideas in <strong>Module 2: Relational Database Design</strong> using simple language;</li><li>apply the main method or rules to a worked example;</li><li>connect the topic to a practical computing scenario; and</li><li>answer short and long university questions with clear steps.</li></ul><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><p>Complete <a href="./Module-1">Module 1</a> first. Review its quick-revision section if any term below feels unfamiliar.</p><h2 id="study-blocks" tabindex="-1">Study blocks <a class="header-anchor" href="#study-blocks" aria-label="Permalink to &quot;Study blocks&quot;">​</a></h2><p>Study one block at a time. Work through its example and checkpoint before continuing.</p><table tabindex="0"><thead><tr><th>Block</th><th>Topic</th><th>Suggested time</th></tr></thead><tbody><tr><td>1</td><td>Start here: the simple idea</td><td>10-15 minutes</td></tr><tr><td>2</td><td>Why this matters in practice</td><td>10-15 minutes</td></tr><tr><td>3</td><td>Important terms explained simply</td><td>10-15 minutes</td></tr><tr><td>4</td><td>1. Why good design matters</td><td>10-15 minutes</td></tr><tr><td>5</td><td>2. Functional dependencies</td><td>10-15 minutes</td></tr><tr><td>6</td><td>3. Decomposition quality</td><td>10-15 minutes</td></tr><tr><td>7</td><td>4. Normal forms</td><td>10-15 minutes</td></tr><tr><td>8</td><td>5. Normalization procedure</td><td>10-15 minutes</td></tr><tr><td>9</td><td>Practice</td><td>10-15 minutes</td></tr><tr><td>10</td><td>Answer</td><td>10-15 minutes</td></tr></tbody></table><h2 id="start-here-the-simple-idea" tabindex="-1">Start here: the simple idea <a class="header-anchor" href="#start-here-the-simple-idea" aria-label="Permalink to &quot;Start here: the simple idea&quot;">​</a></h2><p>Database design is like arranging school information into separate registers. If a student&#39;s row contains the student name, department name, department phone, course name, and teacher name, the same facts will be written many times. Repetition causes mistakes.</p><p><strong>Normalization</strong> means splitting one badly arranged table into smaller connected tables so that each fact is stored in the proper place.</p><h3 id="the-three-common-problems" tabindex="-1">The three common problems <a class="header-anchor" href="#the-three-common-problems" aria-label="Permalink to &quot;The three common problems&quot;">​</a></h3><ul><li><strong>Insert problem:</strong> we cannot add a new department until at least one student joins it.</li><li><strong>Update problem:</strong> a department name appears in 100 rows, so it must be changed 100 times.</li><li><strong>Delete problem:</strong> deleting the last student accidentally deletes the only department information.</li></ul><h3 id="functional-dependency-made-easy" tabindex="-1">Functional dependency, made easy <a class="header-anchor" href="#functional-dependency-made-easy" aria-label="Permalink to &quot;Functional dependency, made easy&quot;">​</a></h3><p><code>StudentID -&gt; StudentName</code> means: if we know the student ID, exactly one student name is decided. The left side is the <strong>determinant</strong>.</p><p>Normalization levels can be remembered like this:</p><ol><li><strong>1NF:</strong> one value in one cell. Do not put <code>9876, 8765</code> together as two phone numbers.</li><li><strong>2NF:</strong> every non-key detail must depend on the <strong>whole</strong> composite key, not only one part.</li><li><strong>3NF:</strong> a non-key detail must not depend on another non-key detail.</li><li><strong>BCNF:</strong> anything that determines another value must be a candidate key.</li><li><strong>4NF:</strong> do not mix two independent multi-value facts, such as hobbies and languages, in one table.</li></ol><p>The goal is not to create the largest number of tables. The goal is to avoid repeated facts while still being able to join the tables correctly.</p><h2 id="why-this-matters-in-practice" tabindex="-1">Why this matters in practice <a class="header-anchor" href="#why-this-matters-in-practice" aria-label="Permalink to &quot;Why this matters in practice&quot;">​</a></h2><ul><li>In textbooks, normalization is the main tool for removing redundancy and anomalies.</li><li>In industry, it helps keep billing, student, order, and inventory systems consistent and easier to maintain.</li><li>The beginner-friendly rule is simple: store each fact once, in the table where it belongs.</li></ul><h2 id="important-terms-explained-simply" tabindex="-1">Important terms explained simply <a class="header-anchor" href="#important-terms-explained-simply" aria-label="Permalink to &quot;Important terms explained simply&quot;">​</a></h2><h3 id="composite-key" tabindex="-1">Composite key <a class="header-anchor" href="#composite-key" aria-label="Permalink to &quot;Composite key&quot;">​</a></h3><p>A <strong>composite key</strong> is a key made from two or more columns together.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ENROLLMENT(StudentID, CourseID, Marks)</span></span>
<span class="line"><span>Key = (StudentID, CourseID)</span></span></code></pre></div><p><code>StudentID</code> alone repeats because one student studies many courses. <code>CourseID</code> alone repeats because one course has many students. Together they identify one enrolment.</p><h3 id="partial-dependency-and-2nf" tabindex="-1">Partial dependency and 2NF <a class="header-anchor" href="#partial-dependency-and-2nf" aria-label="Permalink to &quot;Partial dependency and 2NF&quot;">​</a></h3><p>Consider:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ENROLLMENT(StudentID, CourseID, StudentName, CourseName, Marks)</span></span>
<span class="line"><span>Key = (StudentID, CourseID)</span></span></code></pre></div><ul><li><code>StudentName</code> depends only on <code>StudentID</code>.</li><li><code>CourseName</code> depends only on <code>CourseID</code>.</li><li><code>Marks</code> depends on both <code>StudentID</code> and <code>CourseID</code>.</li></ul><p>The first two are <strong>partial dependencies</strong>, so the table is not in 2NF. Split it into <code>STUDENT</code>, <code>COURSE</code>, and <code>ENROLLMENT</code>.</p><p><strong>Remember:</strong> 2NF means “depend on the whole key, not half of the key.”</p><h3 id="transitive-dependency-and-3nf" tabindex="-1">Transitive dependency and 3NF <a class="header-anchor" href="#transitive-dependency-and-3nf" aria-label="Permalink to &quot;Transitive dependency and 3NF&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>StudentID -&gt; DepartmentID</span></span>
<span class="line"><span>DepartmentID -&gt; DepartmentName</span></span></code></pre></div><p><code>DepartmentName</code> is reached indirectly through <code>DepartmentID</code>. This is a <strong>transitive dependency</strong>. Move department details into a <code>DEPARTMENT</code> table.</p><p><strong>Remember:</strong> 3NF means “non-key details should not depend on other non-key details.”</p><h3 id="lossless-join" tabindex="-1">Lossless join <a class="header-anchor" href="#lossless-join" aria-label="Permalink to &quot;Lossless join&quot;">​</a></h3><p>After splitting a table, a <strong>lossless join</strong> means joining the smaller tables recreates the correct original information without inventing or losing rows.</p><h2 id="_1-why-good-design-matters" tabindex="-1">1. Why good design matters <a class="header-anchor" href="#_1-why-good-design-matters" aria-label="Permalink to &quot;1. Why good design matters&quot;">​</a></h2><p>A poor relation repeats facts and creates:</p><ul><li><strong>Insertion anomaly:</strong> a department cannot be stored until a student exists.</li><li><strong>Update anomaly:</strong> changing a department name requires many updates.</li><li><strong>Deletion anomaly:</strong> deleting the last student accidentally removes department facts.</li></ul><p>Design guidelines are to give every relation a clear meaning, minimize redundancy and nulls, prevent spurious tuples, and preserve important dependencies.</p><h2 id="_2-functional-dependencies" tabindex="-1">2. Functional dependencies <a class="header-anchor" href="#_2-functional-dependencies" aria-label="Permalink to &quot;2. Functional dependencies&quot;">​</a></h2><p>For relation <code>R</code>, the functional dependency <strong>X → Y</strong> holds when any two legal tuples equal on attribute set X must also equal on Y. X is the determinant.</p><p>Example: in <code>STUDENT(StudentID, Name, Email, DepartmentID)</code>,</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>StudentID → Name, Email, DepartmentID</span></span>
<span class="line"><span>Email → StudentID</span></span></code></pre></div><p>Types:</p><ul><li><strong>Trivial:</strong> <code>Y ⊆ X</code>, e.g. <code>{A,B} → A</code></li><li><strong>Non-trivial:</strong> <code>Y</code> is not a subset of <code>X</code></li><li><strong>Full dependency:</strong> Y depends on all of a composite determinant</li><li><strong>Partial dependency:</strong> Y depends on only part of a candidate key</li><li><strong>Transitive dependency:</strong> X → Y and Y → Z imply X → Z, where Y is not a key</li></ul><h3 id="armstrong-s-axioms" tabindex="-1">Armstrong&#39;s axioms <a class="header-anchor" href="#armstrong-s-axioms" aria-label="Permalink to &quot;Armstrong&#39;s axioms&quot;">​</a></h3><p>These sound and complete inference rules derive all implied FDs:</p><ol><li>Reflexivity: if <code>Y ⊆ X</code>, then <code>X → Y</code>.</li><li>Augmentation: if <code>X → Y</code>, then <code>XZ → YZ</code>.</li><li>Transitivity: if <code>X → Y</code> and <code>Y → Z</code>, then <code>X → Z</code>.</li></ol><p>Derived rules include union, decomposition, and pseudotransitivity.</p><h3 id="attribute-closure" tabindex="-1">Attribute closure <a class="header-anchor" href="#attribute-closure" aria-label="Permalink to &quot;Attribute closure&quot;">​</a></h3><p><code>X+</code> is the set of attributes functionally determined by X.</p><p>Algorithm:</p><ol><li>Set <code>X+ = X</code>.</li><li>For each FD <code>Y → Z</code>, if <code>Y ⊆ X+</code>, add Z to <code>X+</code>.</li><li>Repeat until no attribute is added.</li></ol><p>X is a super key if <code>X+</code> contains every attribute of R. It is a candidate key if no proper subset is a super key.</p><p>Example: <code>R(A,B,C,D)</code> with <code>A→B</code>, <code>B→C</code>, <code>AC→D</code>. Start <code>A+={A}</code>, add B, then C, then D because AC is now present. Thus A is a candidate key.</p><h2 id="_3-decomposition-quality" tabindex="-1">3. Decomposition quality <a class="header-anchor" href="#_3-decomposition-quality" aria-label="Permalink to &quot;3. Decomposition quality&quot;">​</a></h2><p>A decomposition should have:</p><ul><li><strong>Lossless join:</strong> joining the decomposed relations always reconstructs exactly the original relation.</li><li><strong>Dependency preservation:</strong> all original constraints can be checked without joining relations.</li></ul><p>For binary decomposition of R into R1 and R2, it is lossless with respect to F if <code>(R1 ∩ R2) → R1</code> or <code>(R1 ∩ R2) → R2</code> follows from F.</p><p>A <strong>spurious tuple</strong> is an incorrect tuple produced when a lossy decomposition is joined.</p><h2 id="_4-normal-forms" tabindex="-1">4. Normal forms <a class="header-anchor" href="#_4-normal-forms" aria-label="Permalink to &quot;4. Normal forms&quot;">​</a></h2><h3 id="first-normal-form-1nf" tabindex="-1">First Normal Form (1NF) <a class="header-anchor" href="#first-normal-form-1nf" aria-label="Permalink to &quot;First Normal Form (1NF)&quot;">​</a></h3><p>Every cell contains one atomic value; there are no repeating groups. Move multiple phone numbers to <code>STUDENT_PHONE(StudentID, Phone)</code>.</p><h3 id="second-normal-form-2nf" tabindex="-1">Second Normal Form (2NF) <a class="header-anchor" href="#second-normal-form-2nf" aria-label="Permalink to &quot;Second Normal Form (2NF)&quot;">​</a></h3><p>R is in 2NF when it is in 1NF and every non-prime attribute is fully dependent on every candidate key. It matters when a key is composite.</p><p>Example:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ENROLLMENT(StudentID, CourseID, StudentName, CourseTitle, Grade)</span></span>
<span class="line"><span>key = (StudentID, CourseID)</span></span>
<span class="line"><span>StudentID → StudentName</span></span>
<span class="line"><span>CourseID → CourseTitle</span></span></code></pre></div><p>Decompose into <code>STUDENT</code>, <code>COURSE</code>, and <code>ENROLLMENT(StudentID, CourseID, Grade)</code>.</p><h3 id="third-normal-form-3nf" tabindex="-1">Third Normal Form (3NF) <a class="header-anchor" href="#third-normal-form-3nf" aria-label="Permalink to &quot;Third Normal Form (3NF)&quot;">​</a></h3><h4 id="simple-meaning" tabindex="-1">Simple meaning <a class="header-anchor" href="#simple-meaning" aria-label="Permalink to &quot;Simple meaning&quot;">​</a></h4><p>In 3NF, every non-key attribute must depend <strong>directly on a candidate key</strong>. It should not depend on the key indirectly through another non-key attribute.</p><p>In simple words:</p><blockquote><p>A non-key column must not determine another non-key column.</p></blockquote><h4 id="technical-definition" tabindex="-1">Technical definition <a class="header-anchor" href="#technical-definition" aria-label="Permalink to &quot;Technical definition&quot;">​</a></h4><p>A relation <code>R</code> is in <strong>Third Normal Form (3NF)</strong> if, for every non-trivial functional dependency:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>X → A</span></span></code></pre></div><p>at least one of the following conditions is true:</p><ol><li><code>X</code> is a <strong>super key</strong>, or</li><li><code>A</code> is a <strong>prime attribute</strong>, meaning it belongs to at least one candidate key.</li></ol><p>A functional dependency is <strong>non-trivial</strong> when <code>A</code> is not already a part of <code>X</code>.</p><h4 id="example" tabindex="-1">Example <a class="header-anchor" href="#example" aria-label="Permalink to &quot;Example&quot;">​</a></h4><p>Consider:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>STUDENT</span></span>
<span class="line"><span>StudentID | StudentName | DepartmentID | DepartmentName</span></span>
<span class="line"><span>101       | Asha        | D1           | Computer Science</span></span>
<span class="line"><span>102       | Ravi        | D2           | Commerce</span></span>
<span class="line"><span>103       | Neha        | D1           | Computer Science</span></span></code></pre></div><p>Assume <code>StudentID</code> is the primary key. The functional dependencies are:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>StudentID    → StudentName</span></span>
<span class="line"><span>StudentID    → DepartmentID</span></span>
<span class="line"><span>DepartmentID → DepartmentName</span></span></code></pre></div><p>Therefore:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>StudentID → DepartmentID → DepartmentName</span></span></code></pre></div><p><code>DepartmentName</code> depends on <code>StudentID</code> indirectly through <code>DepartmentID</code>. This is called a <strong>transitive dependency</strong>.</p><p>The dependency:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DepartmentID → DepartmentName</span></span></code></pre></div><p>violates 3NF because:</p><ul><li><code>DepartmentID</code> is not a super key of the <code>STUDENT</code> table.</li><li><code>DepartmentName</code> is not a prime attribute.</li></ul><h4 id="convert-the-table-to-3nf" tabindex="-1">Convert the table to 3NF <a class="header-anchor" href="#convert-the-table-to-3nf" aria-label="Permalink to &quot;Convert the table to 3NF&quot;">​</a></h4><p>Split it into two relations:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>STUDENT(StudentID, StudentName, DepartmentID)</span></span>
<span class="line"><span>Primary key: StudentID</span></span>
<span class="line"><span>Foreign key: DepartmentID</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DEPARTMENT(DepartmentID, DepartmentName)</span></span>
<span class="line"><span>Primary key: DepartmentID</span></span></code></pre></div><p>Now:</p><ul><li><code>StudentName</code> and <code>DepartmentID</code> depend directly on <code>StudentID</code>.</li><li><code>DepartmentName</code> depends directly on <code>DepartmentID</code>.</li><li>The transitive dependency has been removed.</li></ul><h4 id="why-is-3nf-useful" tabindex="-1">Why is 3NF useful? <a class="header-anchor" href="#why-is-3nf-useful" aria-label="Permalink to &quot;Why is 3NF useful?&quot;">​</a></h4><p>Suppose the department name changes from <code>Computer Science</code> to <code>Computer Applications</code>.</p><p>In the original table, it may appear in hundreds of student rows and must be updated many times. After normalization, it is stored once in <code>DEPARTMENT</code>, so only one row needs to be updated.</p><p>This prevents:</p><ul><li>Repeated department information</li><li>Inconsistent department names</li><li>Update anomalies</li><li>Accidental loss of department information</li></ul><h4 id="memory-rule" tabindex="-1">Memory rule <a class="header-anchor" href="#memory-rule" aria-label="Permalink to &quot;Memory rule&quot;">​</a></h4><blockquote><p>Every non-key attribute must depend on the key, the whole key, and nothing but the key.</p></blockquote><ul><li><strong>The key:</strong> the table must have a proper identifier.</li><li><strong>The whole key:</strong> no partial dependency; this is mainly the 2NF requirement.</li><li><strong>Nothing but the key:</strong> no transitive dependency; this is the 3NF requirement.</li></ul><h3 id="boyce-codd-normal-form-bcnf" tabindex="-1">Boyce-Codd Normal Form (BCNF) <a class="header-anchor" href="#boyce-codd-normal-form-bcnf" aria-label="Permalink to &quot;Boyce-Codd Normal Form (BCNF)&quot;">​</a></h3><p>For every non-trivial FD <code>X → Y</code>, X must be a super key. BCNF is stricter than 3NF. A 3NF relation can violate BCNF when a determinant is not a super key but the dependent attribute is prime.</p><p>Example: <code>TEACH(Student, Subject, Teacher)</code> with <code>(Student, Subject)→Teacher</code> and <code>Teacher→Subject</code>. Candidate keys are <code>(Student,Subject)</code> and <code>(Student,Teacher)</code>. It satisfies 3NF, but <code>Teacher→Subject</code> violates BCNF.</p><p>BCNF decomposition is lossless but may not preserve all dependencies.</p><h3 id="fourth-normal-form-4nf" tabindex="-1">Fourth Normal Form (4NF) <a class="header-anchor" href="#fourth-normal-form-4nf" aria-label="Permalink to &quot;Fourth Normal Form (4NF)&quot;">​</a></h3><p>A multivalued dependency <code>X ↠ Y</code> means that for a fixed X, Y values vary independently of the remaining attributes. R is in 4NF if, for every non-trivial MVD <code>X ↠ Y</code>, X is a super key.</p><p>Example: if a student independently has several hobbies and languages:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>STUDENT_INFO(Student, Hobby, Language)</span></span></code></pre></div><p>decompose into <code>STUDENT_HOBBY(Student,Hobby)</code> and <code>STUDENT_LANGUAGE(Student,Language)</code>.</p><h2 id="_5-normalization-procedure" tabindex="-1">5. Normalization procedure <a class="header-anchor" href="#_5-normalization-procedure" aria-label="Permalink to &quot;5. Normalization procedure&quot;">​</a></h2><ol><li>List attributes, candidate keys, and valid FDs.</li><li>Convert repeating groups to 1NF.</li><li>Remove partial dependencies for 2NF.</li><li>Remove transitive dependencies for 3NF.</li><li>Check every determinant for BCNF.</li><li>Identify independent multivalued facts for 4NF.</li><li>Verify lossless join and dependency preservation.</li></ol><p>Normalization improves consistency but can increase joins. <strong>Denormalization</strong> deliberately introduces controlled redundancy for proven performance needs; it requires a strategy to preserve consistency.</p><h2 id="exam-focus" tabindex="-1">Exam focus <a class="header-anchor" href="#exam-focus" aria-label="Permalink to &quot;Exam focus&quot;">​</a></h2><ul><li>Normalization is based on business-rule dependencies, not sample data alone.</li><li>2NF concerns partial dependency on a composite candidate key.</li><li>3NF permits <code>X→A</code> when A is prime; BCNF does not.</li><li>Show every decomposition and identify its primary/foreign keys.</li></ul><h2 id="common-mistakes" tabindex="-1">Common mistakes <a class="header-anchor" href="#common-mistakes" aria-label="Permalink to &quot;Common mistakes&quot;">​</a></h2><ul><li>Normalizing from a few sample rows instead of business rules</li><li>Checking 2NF when the key has only one attribute</li><li>Confusing partial dependency with transitive dependency</li><li>Splitting tables without checking whether the join is lossless</li></ul><h2 id="memory-rules" tabindex="-1">Memory rules <a class="header-anchor" href="#memory-rules" aria-label="Permalink to &quot;Memory rules&quot;">​</a></h2><ul><li>2NF: depend on the whole composite key.</li><li>3NF: non-key details depend on no other non-key detail.</li><li>BCNF: every determinant is a super key.</li><li>Lossless means the correct original rows can be rebuilt.</li></ul><h2 id="practice" tabindex="-1">Practice <a class="header-anchor" href="#practice" aria-label="Permalink to &quot;Practice&quot;">​</a></h2><p>Given <code>ORDER(OrderNo, OrderDate, CustomerID, CustomerName, ProductID, ProductName, Qty)</code> and key <code>(OrderNo, ProductID)</code>, normalize it to 3NF and state the FDs used.</p><h2 id="answer" tabindex="-1">Answer <a class="header-anchor" href="#answer" aria-label="Permalink to &quot;Answer&quot;">​</a></h2><p>Use these dependencies:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OrderNo → OrderDate, CustomerID</span></span>
<span class="line"><span>CustomerID → CustomerName</span></span>
<span class="line"><span>ProductID → ProductName</span></span>
<span class="line"><span>(OrderNo, ProductID) → Qty</span></span></code></pre></div><p>The 3NF relations are:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CUSTOMER(CustomerID, CustomerName)</span></span>
<span class="line"><span>PRODUCT(ProductID, ProductName)</span></span>
<span class="line"><span>ORDER_HEADER(OrderNo, OrderDate, CustomerID)</span></span>
<span class="line"><span>ORDER_ITEM(OrderNo, ProductID, Qty)</span></span></code></pre></div><h2 id="quick-revision-box" tabindex="-1">Quick revision box <a class="header-anchor" href="#quick-revision-box" aria-label="Permalink to &quot;Quick revision box&quot;">​</a></h2><ul><li>1NF: one value per cell.</li><li>2NF: no dependency on only part of a composite key.</li><li>3NF: no non-key attribute depends on another non-key attribute.</li><li>BCNF: every determinant must be a super key.</li><li>4NF: separate independent multivalued facts.</li><li>A good decomposition should be lossless and preferably dependency-preserving.</li></ul><h2 id="mini-quiz" tabindex="-1">Mini Quiz <a class="header-anchor" href="#mini-quiz" aria-label="Permalink to &quot;Mini Quiz&quot;">​</a></h2><ol><li>Explain the module&#39;s main idea in your own words.</li><li>Apply one method from this module to a small example.</li><li>Identify one common mistake and explain how to avoid it.</li></ol><details><summary>Reveal evaluation guide</summary><p>A strong response uses the correct terms, shows the method in steps, and checks or justifies the result.</p></details><h2 id="practice-ladder" tabindex="-1">Practice ladder <a class="header-anchor" href="#practice-ladder" aria-label="Permalink to &quot;Practice ladder&quot;">​</a></h2><ol><li><strong>Easy - Recall:</strong> Define the module&#39;s central idea in one or two sentences.</li><li><strong>Easy - Recognize:</strong> Identify the correct method for a small example and explain why it fits.</li><li><strong>Medium - Apply:</strong> Work through one representative problem without copying the example.</li><li><strong>Medium - Compare:</strong> Contrast two methods or concepts from the module.</li><li><strong>Hard - Integrate:</strong> Solve a university-style scenario and justify every major step.</li></ol><details><summary>Reveal self-evaluation guide</summary><p>A complete response uses correct terminology, shows intermediate steps, connects the result to the scenario, and states one assumption or limitation.</p></details><hr><p><a href="./Module-1">← Previous: Module 1</a> · <a href="./README">Subject index</a> · <a href="./Module-3">Next: Module 3 →</a></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/persistent-data-management/Module-2.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module2 = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module2 as default
};
