import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 10: System Interface and Execution","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Module-10.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Module-10.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Module-10.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-10-system-interface-and-execution" tabindex="-1">Module 10: System Interface and Execution <a class="header-anchor" href="#module-10-system-interface-and-execution" aria-label="Permalink to &quot;Module 10: System Interface and Execution&quot;">​</a></h1><p><a href="./Module-9">← Previous: Module 9</a> · <a href="./README">Subject index</a> · <a href="./MCQ">MCQ practice →</a></p><h2 id="learning-outcomes" tabindex="-1">Learning outcomes <a class="header-anchor" href="#learning-outcomes" aria-label="Permalink to &quot;Learning outcomes&quot;">​</a></h2><p>After this module, you should be able to:</p><ul><li>distinguish API and ABI and explain why each matters;</li><li>describe system calls and how programs request services from the OS;</li><li>explain protected mode and driver interactions;</li><li>describe startup scripts and the bootstrapping process;</li><li>identify common architectural (ABI/API) compatibility issues.</li></ul><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><p>Complete <a href="./Module-9">Module 9</a> first. Review its quick-revision section if any term below feels unfamiliar.</p><h2 id="study-blocks" tabindex="-1">Study blocks <a class="header-anchor" href="#study-blocks" aria-label="Permalink to &quot;Study blocks&quot;">​</a></h2><p>Study one block at a time. Work through its example and checkpoint before continuing.</p><table tabindex="0"><thead><tr><th>Block</th><th>Topic</th><th>Suggested time</th></tr></thead><tbody><tr><td>1</td><td>Start here: the simple idea</td><td>10-15 minutes</td></tr><tr><td>2</td><td>API vs ABI: definition and distinction</td><td>10-15 minutes</td></tr><tr><td>3</td><td>Decoding architectural issues</td><td>10-15 minutes</td></tr><tr><td>4</td><td>Role of system calls (overview)</td><td>10-15 minutes</td></tr><tr><td>5</td><td>API vs ABI theory</td><td>10-15 minutes</td></tr><tr><td>6</td><td>Protected mode basics and driver interactions</td><td>10-15 minutes</td></tr><tr><td>7</td><td>Startup scripts and system bootstrapping</td><td>10-15 minutes</td></tr></tbody></table><h2 id="start-here-the-simple-idea" tabindex="-1">Start here: the simple idea <a class="header-anchor" href="#start-here-the-simple-idea" aria-label="Permalink to &quot;Start here: the simple idea&quot;">​</a></h2><p>When software talks to the operating system or to hardware, it uses agreed <strong>interfaces</strong>. A <strong>system call</strong> is the program&#39;s request to the OS (&quot;open this file,&quot; &quot;allocate memory&quot;). The <strong>API</strong> and <strong>ABI</strong> are contracts that let programs and libraries work together across compilers and even different operating systems.</p><h3 id="everyday-analogy" tabindex="-1">Everyday analogy <a class="header-anchor" href="#everyday-analogy" aria-label="Permalink to &quot;Everyday analogy&quot;">​</a></h3><ul><li>An <strong>API</strong> is like a restaurant menu: it lists dishes (functions) and what you get, without showing the kitchen.</li><li>An <strong>ABI</strong> is like the exact plate size, cutlery placement, and payment format the kitchen expects — if the format changes, old waiters break.</li></ul><h2 id="api-vs-abi-definition-and-distinction" tabindex="-1">API vs ABI: definition and distinction <a class="header-anchor" href="#api-vs-abi-definition-and-distinction" aria-label="Permalink to &quot;API vs ABI: definition and distinction&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Aspect</th><th>API (Application Programming Interface)</th><th>ABI (Application Binary Interface)</th></tr></thead><tbody><tr><td>Level</td><td>Source code</td><td>Binary</td></tr><tr><td>Checked when</td><td>Compile/link</td><td>Load/run</td></tr><tr><td>Covers</td><td>Function names, arguments, return types, headers</td><td>Calling convention, register usage, name mangling, struct layout, library format</td></tr><tr><td>Example change</td><td>Rename a parameter (if types stay same) — still compiles</td><td>Change struct field order — already-compiled .so breaks</td></tr><tr><td>Stability goal</td><td>Readable, documented, evolvable</td><td>Backward-compatible binary contract</td></tr></tbody></table><p><strong>Example:</strong> A C library exposes <code>int add(int, int);</code> (API). The ABI also fixes that arguments go in registers/stack in a specific order, that <code>int</code> is 32 bits, and that the symbol is named <code>add</code> (or <code>__add</code> if mangled). A C++ compiler mangles names differently, so C++ cannot link to a C library unless the ABI is agreed at the boundary (<code>extern &quot;C&quot;</code>).</p><h2 id="decoding-architectural-issues" tabindex="-1">Decoding architectural issues <a class="header-anchor" href="#decoding-architectural-issues" aria-label="Permalink to &quot;Decoding architectural issues&quot;">​</a></h2><h3 id="part-1-calling-convention" tabindex="-1">Part 1: Calling convention <a class="header-anchor" href="#part-1-calling-convention" aria-label="Permalink to &quot;Part 1: Calling convention&quot;">​</a></h3><p>The <strong>calling convention</strong> defines <em>how</em> arguments and return values are passed: which registers vs stack, who cleans the stack (callee vs caller), and which registers must be preserved. Differences (e.g., x86-32 <code>stdcall</code> vs <code>x86-64 System V</code>) break binary compatibility.</p><h3 id="part-2-data-representation" tabindex="-1">Part 2: Data representation <a class="header-anchor" href="#part-2-data-representation" aria-label="Permalink to &quot;Part 2: Data representation&quot;">​</a></h3><ul><li><strong>Endianness:</strong> byte order. Little-endian (x86): least-significant byte first. Big-endian (network byte order, classic ARM): most-significant byte first.</li><li><strong>Alignment &amp; padding:</strong> <code>struct { char; int; }</code> may be padded to 8 bytes; changing it changes the binary layout.</li><li><strong>Size of types:</strong> <code>int</code>, <code>long</code>, and pointers differ across 32/64-bit ABIs (the classic &quot;LLP64 vs LP64&quot; problem).</li></ul><h2 id="role-of-system-calls-overview" tabindex="-1">Role of system calls (overview) <a class="header-anchor" href="#role-of-system-calls-overview" aria-label="Permalink to &quot;Role of system calls (overview)&quot;">​</a></h2><p>A <strong>system call</strong> is the controlled entry point into the kernel. Common calls:</p><table tabindex="0"><thead><tr><th>Call</th><th>Purpose</th></tr></thead><tbody><tr><td><code>read</code> / <code>write</code></td><td>I/O on file descriptors</td></tr><tr><td><code>open</code> / <code>close</code></td><td>file access</td></tr><tr><td><code>fork</code> / <code>exec</code> / <code>wait</code></td><td>process control</td></tr><tr><td><code>mmap</code> / <code>brk</code></td><td>memory management</td></tr><tr><td><code>socket</code> / <code>send</code> / <code>recv</code></td><td>networking</td></tr></tbody></table><h3 id="how-a-system-call-works-recap-from-module-9" tabindex="-1">How a system call works (recap from Module 9) <a class="header-anchor" href="#how-a-system-call-works-recap-from-module-9" aria-label="Permalink to &quot;How a system call works (recap from Module 9)&quot;">​</a></h3><p>The program sets up arguments, executes a privileged instruction (<code>syscall</code>/<code>int 0x80</code>/<code>svc #0</code>), the CPU switches to kernel mode and dispatches to the syscall handler, the OS performs the service, and returns the result. This is the <strong>same exception/trap mechanism</strong>; the syscall is a <em>deliberate</em> trap.</p><h2 id="api-vs-abi-theory" tabindex="-1">API vs ABI theory <a class="header-anchor" href="#api-vs-abi-theory" aria-label="Permalink to &quot;API vs ABI theory&quot;">​</a></h2><p>A stable API is easy to keep readable; a stable ABI is harder because binaries are frozen. Strategies:</p><ul><li><strong>Symbol versioning:</strong> keep old symbol names while providing new ones (glibc <code>GLIBC_2.2.5</code>, etc.).</li><li><strong>Deprecation without removal:</strong> mark old APIs deprecated but keep them.</li><li><strong>Wrapper/compat layer:</strong> translate old ABIs to new ones.</li><li><strong>Avoid opaque struct layout:</strong> use accessors, not direct field access, across the ABI boundary.</li></ul><h2 id="protected-mode-basics-and-driver-interactions" tabindex="-1">Protected mode basics and driver interactions <a class="header-anchor" href="#protected-mode-basics-and-driver-interactions" aria-label="Permalink to &quot;Protected mode basics and driver interactions&quot;">​</a></h2><h3 id="protected-mode" tabindex="-1">Protected mode <a class="header-anchor" href="#protected-mode" aria-label="Permalink to &quot;Protected mode&quot;">​</a></h3><p>In <strong>protected mode</strong> (x86 term), the CPU uses segmentation and paging to give each process a <strong>virtual address space</strong> and enforce <strong>protection</strong>:</p><ul><li><strong>Segmentation:</strong> base + limit per segment → bounds checking.</li><li><strong>Paging:</strong> virtual → physical translation, per-page protection.</li><li><strong>Privilege levels (rings 0–3):</strong> only ring-0 code can execute privileged instructions (I/O, CLI/STI, HLT, etc.).</li></ul><p>Protected mode is what makes the user/kernel separation (Module 9) possible and is the foundation of virtual memory (Module 8).</p><h3 id="drivers" tabindex="-1">Drivers <a class="header-anchor" href="#drivers" aria-label="Permalink to &quot;Drivers&quot;">​</a></h3><p>A <strong>device driver</strong> is the kernel module that talks to hardware on behalf of user programs. Interactions:</p><ol><li>User opens <code>/dev/&lt;name&gt;</code> and issues <code>read</code>/<code>write</code>/<code>ioctl</code>.</li><li>The OS routes the request to the driver&#39;s file operations.</li><li>The driver programs the device (I/O ports / MMIO registers), possibly via DMA, and may use interrupts (Module 9) to know when I/O completes.</li><li>The driver returns data or a status.</li></ol><p>Drivers run in <strong>kernel mode</strong> (ring 0) because hardware access is privileged; this is also why a buggy driver can crash the whole system.</p><h2 id="startup-scripts-and-system-bootstrapping" tabindex="-1">Startup scripts and system bootstrapping <a class="header-anchor" href="#startup-scripts-and-system-bootstrapping" aria-label="Permalink to &quot;Startup scripts and system bootstrapping&quot;">​</a></h2><h3 id="bootstrapping-boot" tabindex="-1">Bootstrapping (boot) <a class="header-anchor" href="#bootstrapping-boot" aria-label="Permalink to &quot;Bootstrapping (boot)&quot;">​</a></h3><p>Boot sequence (x86):</p><ol><li><strong>BIOS/UEFI</strong> firmware runs power-on self-test and initializes hardware.</li><li>Firmware reads the <strong>bootloader</strong> from a known device.</li><li>Bootloader loads the <strong>kernel</strong> image and an initial RAM disk (<code>initrd</code>).</li><li>Kernel initializes memory management, devices, mounts <code>rootfs</code>, and starts the first <strong>init</strong> process (PID 1).</li></ol><h3 id="startup-scripts" tabindex="-1">Startup scripts <a class="header-anchor" href="#startup-scripts" aria-label="Permalink to &quot;Startup scripts&quot;">​</a></h3><ul><li><strong>Traditional:</strong> <code>init</code> runs <code>/etc/inittab</code>, then SysV init scripts in <code>/etc/init.d/</code> with runlevels.</li><li><strong>Modern:</strong> <code>systemd</code> starts <strong>units</strong> (services, sockets, timers) in parallel, replacing init scripts. On macOS/BSD, <code>launchd</code>/<code>launchctl</code>.</li></ul><p>Startup scripts configure networking, mount filesystems, and launch daemons and the login shell — the system is now ready for users and services.</p><h2 id="common-mistakes" tabindex="-1">Common mistakes <a class="header-anchor" href="#common-mistakes" aria-label="Permalink to &quot;Common mistakes&quot;">​</a></h2><ul><li>Confusing API (source) with ABI (binary): a source-compatible change can still break the ABI.</li><li>Ignoring endianness when reading multi-byte data across machines.</li><li>Writing drivers that block or use non-reentrant code (like ISRs, they must be careful).</li><li>Assuming protected mode is optional — virtually all modern OSes run in it.</li></ul><h2 id="memory-rules" tabindex="-1">Memory rules <a class="header-anchor" href="#memory-rules" aria-label="Permalink to &quot;Memory rules&quot;">​</a></h2><ul><li>API = source contract; ABI = binary contract.</li><li>Calling convention + struct layout + type sizes define the ABI.</li><li>System call = deliberate trap into the kernel (privileged service).</li><li>Protected mode = virtual addresses + privilege levels + paging.</li><li>Driver = kernel module that owns hardware access for a device.</li><li>Boot = firmware → bootloader → kernel → init → startup scripts.</li></ul><h2 id="check-your-understanding" tabindex="-1">Check your understanding <a class="header-anchor" href="#check-your-understanding" aria-label="Permalink to &quot;Check your understanding&quot;">​</a></h2><ol><li>What is one change that can break an ABI but not the API?</li><li>How does a system call enter the kernel, at the hardware level?</li><li>Why must drivers run in kernel mode?</li><li>Name the four stages of the PC boot sequence in order.</li><li>What problem does symbol versioning solve?</li></ol><h2 id="answers" tabindex="-1">Answers <a class="header-anchor" href="#answers" aria-label="Permalink to &quot;Answers&quot;">​</a></h2><details><summary>Reveal answers after attempting the questions</summary><ol><li>Changing the layout of a <code>struct</code> (field order/ padding) breaks the ABI for code compiled against the old layout while the API (function name + types) stays the same.</li><li>The program executes a privileged instruction (e.g., <code>syscall</code>/<code>int 0x80</code>) which traps to kernel mode and dispatches via the interrupt vector to the syscall handler.</li><li>Hardware access (I/O ports, MMIO, privileged instructions) is restricted to ring 0; user mode cannot touch devices directly.</li><li>Firmware (BIOS/UEFI) → bootloader → kernel → init (PID 1), after which startup scripts bring up services.</li><li>Symbol versioning lets a library keep old binary symbols for old programs while exposing new symbols to new programs, preserving backward ABI compatibility.</li></ol></details><h2 id="quick-revision-box" tabindex="-1">Quick revision box <a class="header-anchor" href="#quick-revision-box" aria-label="Permalink to &quot;Quick revision box&quot;">​</a></h2><ul><li>API = source contract; ABI = binary contract (calling convention, layout, sizes).</li><li>System call = deliberate trap into the kernel; same hardware as interrupts.</li><li>Protected mode = virtual memory + privilege rings (only ring 0 uses privileged I/O).</li><li>Drivers = kernel modules owning hardware; interact via file ops (<code>read</code>/<code>ioctl</code>).</li><li>Boot: firmware → bootloader → kernel → init → startup scripts (systemd/init.d).</li></ul><h2 id="exam-guidance" tabindex="-1">Exam guidance <a class="header-anchor" href="#exam-guidance" aria-label="Permalink to &quot;Exam guidance&quot;">​</a></h2><p>Draw the relevant block or timing diagram, label data movement, show the calculation, and explain the performance consequence.</p><h2 id="practice-ladder" tabindex="-1">Practice ladder <a class="header-anchor" href="#practice-ladder" aria-label="Permalink to &quot;Practice ladder&quot;">​</a></h2><ol><li><strong>Easy - Recall:</strong> Define the module&#39;s central idea in one or two sentences.</li><li><strong>Easy - Recognize:</strong> Identify the correct method for a small example and explain why it fits.</li><li><strong>Medium - Apply:</strong> Work through one representative problem without copying the example.</li><li><strong>Medium - Compare:</strong> Contrast two methods or concepts from the module.</li><li><strong>Hard - Integrate:</strong> Solve a university-style scenario and justify every major step.</li></ol><details><summary>Reveal self-evaluation guide</summary><p>A complete response uses correct terminology, shows intermediate steps, connects the result to the scenario, and states one assumption or limitation.</p></details><hr><p><a href="./Module-9">← Previous: Module 9</a> · <a href="./README">Subject index</a> · <a href="./MCQ">MCQ practice →</a></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Module-10.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module10 = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module10 as default
};
