import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 3 Article: Inside the CPU and the System Bus","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-3-Article.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-3-Article.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Articles/Module-3-Article.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-3-article-inside-the-cpu-and-the-system-bus" tabindex="-1">Module 3 Article: Inside the CPU and the System Bus <a class="header-anchor" href="#module-3-article-inside-the-cpu-and-the-system-bus" aria-label="Permalink to &quot;Module 3 Article: Inside the CPU and the System Bus&quot;">​</a></h1><h2 id="the-big-idea" tabindex="-1">The big idea <a class="header-anchor" href="#the-big-idea" aria-label="Permalink to &quot;The big idea&quot;">​</a></h2><p>A computer is a set of <strong>components</strong> that exchange data over shared <strong>wires (buses)</strong>. The CPU, memory, and I/O are connected by the <strong>system bus</strong>, and a chipset routes traffic between fast and slow parts. Understanding the bus and the CPU&#39;s internal datapath/control split is the key to reading any computer architecture diagram.</p><h2 id="core-components-at-a-glance" tabindex="-1">Core components at a glance <a class="header-anchor" href="#core-components-at-a-glance" aria-label="Permalink to &quot;Core components at a glance&quot;">​</a></h2><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">flowchart LR</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    CPU[CPU: control + datapath] &lt;--&gt; BUS[System bus]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    BUS &lt;--&gt; MEM[Main memory]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    BUS &lt;--&gt; IO[I/O devices]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    CHIP[chipset: north/south bridge]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    BUS &lt;--&gt; CHIP</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    CHIP &lt;--&gt; DEV[PCIe / USB / SATA devices]</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Component</th><th>One-line role</th></tr></thead><tbody><tr><td>CPU</td><td>Fetches, decodes, and executes instructions</td></tr><tr><td>Memory</td><td>Holds program + data while running</td></tr><tr><td>I/O</td><td>Connects keyboard, disk, network, screen</td></tr><tr><td>System bus</td><td>Shared wires for data, address, control</td></tr><tr><td>Chipset</td><td>Arbitrates between CPU/memory and peripherals</td></tr></tbody></table><h2 id="the-system-bus-in-detail" tabindex="-1">The system bus in detail <a class="header-anchor" href="#the-system-bus-in-detail" aria-label="Permalink to &quot;The system bus in detail&quot;">​</a></h2><p>A bus is a set of wires, but the data bus is <strong>shared</strong>, so only one device may drive it at a time. The system bus has three groups:</p><ul><li><strong>Data bus:</strong> carries data and instructions. <strong>Bidirectional</strong>. Width (e.g. 64 bits) sets the natural word size.</li><li><strong>Address bus:</strong> carries the memory/IO address the CPU wants. Mostly <strong>CPU → rest</strong> (unidirectional). Width sets the addressable range (32-bit bus → 4 GiB).</li><li><strong>Control bus:</strong> <code>READ#</code>, <code>WRITE#</code>, <code>CLK</code>, <code>INTERRUPT#</code>, <code>RESET#</code>. Mostly CPU → rest, plus some bidirectional lines (interrupt request, acknowledge).</li></ul><h2 id="bus-hierarchy-fast-near-slow-far" tabindex="-1">Bus hierarchy: fast near, slow far <a class="header-anchor" href="#bus-hierarchy-fast-near-slow-far" aria-label="Permalink to &quot;Bus hierarchy: fast near, slow far&quot;">​</a></h2><p>A single bus is a bottleneck. Real CPUs use <strong>multiple buses</strong> in a hierarchy:</p><ol><li><strong>Core/private L1/L2:</strong> inside/near the CPU core.</li><li><strong>CPU ↔ memory:</strong> memory channels (DDR) or Infinity Fabric/UPI.</li><li><strong>PCIe lanes:</strong> GPU, NVMe SSD, network — directly from CPU.</li><li><strong>Chipset bus (DMI/PCIe ×4):</strong> southbridge for USB, SATA, audio, etc.</li></ol><p>Each layer trades <strong>speed</strong> (top) vs <strong>distance/breadth</strong> (bottom).</p><h2 id="inside-the-cpu-datapath-and-control" tabindex="-1">Inside the CPU: datapath and control <a class="header-anchor" href="#inside-the-cpu-datapath-and-control" aria-label="Permalink to &quot;Inside the CPU: datapath and control&quot;">​</a></h2><p>The CPU has two inseparable parts:</p><ol><li><strong>Datapath:</strong> registers, buses, multiplexers, ALU.</li><li><strong>Control unit:</strong> generates control signals.</li></ol><p>In MiniCPU&#39;s single-cycle datapath:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Registers → MUXes → ALU → result; Register file writes; PC updates.</span></span>
<span class="line"><span>Control: opcode → control lines (ALUOp, RegDst, ALUSrc, MemRead, MemWrite,</span></span>
<span class="line"><span>RegWrite, MemtoReg, PCSrc).</span></span></code></pre></div><p>The control unit reads the <strong>opcode</strong> field of the instruction and decodes it into control signals. <strong>Hardwired control</strong> = combinational logic; <strong>microprogrammed control</strong> = a microcode ROM holding control words.</p><h2 id="cpu-components-in-a-modern-processor" tabindex="-1">CPU components in a modern processor <a class="header-anchor" href="#cpu-components-in-a-modern-processor" aria-label="Permalink to &quot;CPU components in a modern processor&quot;">​</a></h2><p>A modern CPU core contains far more than the textbook datapath:</p><ul><li><strong>Register file</strong> (e.g., 31 × 64-bit GPRs in AArch64).</li><li><strong>ALU / FPU</strong> for integer and floating-point work.</li><li><strong>L1 caches</strong> (split I-cache and D-cache, ~32–48 KB each).</li><li><strong>L2 cache</strong> (private per core, ~512 KB–1 MB).</li><li><strong>Branch predictor</strong>, <strong>return stack</strong>, and <strong>BTB</strong> to keep the pipeline fed.</li><li><strong>Reorder buffer / reservation stations</strong> for out-of-order execution.</li><li><strong>MMU</strong> for virtual memory (next module).</li></ul><h2 id="arbitration-and-chip-select" tabindex="-1">Arbitration and chip select <a class="header-anchor" href="#arbitration-and-chip-select" aria-label="Permalink to &quot;Arbitration and chip select&quot;">​</a></h2><p>When several devices share a bus, <strong>arbitration</strong> decides who goes next. Two styles:</p><ul><li><strong>Daisy chain:</strong> priority is fixed by position (lower priority devices later).</li><li><strong>Centralized/distributed arbitration:</strong> a dedicated arbiter grants access.</li></ul><p>A <strong>decoder</strong> performs <strong>address decoding</strong>: a device sees the address bus and activates its <strong>chip-select</strong> line only for addresses in its range. That is how the CPU &quot;talks&quot; to one device at a time on a shared bus.</p><h2 id="hardwired-vs-general-purpose" tabindex="-1">Hardwired vs general-purpose <a class="header-anchor" href="#hardwired-vs-general-purpose" aria-label="Permalink to &quot;Hardwired vs general-purpose&quot;">​</a></h2><ul><li>A <strong>hardwired/special-purpose</strong> system (traffic-light controller, embedded microcontroller) runs one fixed program in ROM; no OS, no reprogramming.</li><li>A <strong>general-purpose PC</strong> loads and runs any compatible program from disk under an OS, using standard buses and a programmable ISA.</li></ul><h2 id="exam-angle" tabindex="-1">Exam angle <a class="header-anchor" href="#exam-angle" aria-label="Permalink to &quot;Exam angle&quot;">​</a></h2><p>For a &quot;label the system bus&quot; diagram:</p><ol><li>Identify <strong>data bus</strong> (bidirectional, wider).</li><li>Identify <strong>address bus</strong> (one-directional, CPU → memory).</li><li>Identify <strong>control bus</strong> (read, write, clock, interrupt).</li><li>Mark the <strong>chipset</strong> as the bridge between the CPU/memory side and the peripheral side.</li><li>Add <strong>bus arbiter</strong> if the question mentions multiple bus masters.</li></ol><blockquote><p>Tip: if asked about the &quot;two parts of the CPU,&quot; always say <strong>datapath</strong> and <strong>control unit</strong> — never &quot;ALU and registers&quot; (those belong to the datapath).</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Articles/Module-3-Article.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module3Article = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module3Article as default
};
