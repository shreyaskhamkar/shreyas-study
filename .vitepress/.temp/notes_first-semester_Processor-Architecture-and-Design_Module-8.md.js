import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 8: Virtual Memory Systems","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Module-8.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Module-8.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Module-8.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-8-virtual-memory-systems" tabindex="-1">Module 8: Virtual Memory Systems <a class="header-anchor" href="#module-8-virtual-memory-systems" aria-label="Permalink to &quot;Module 8: Virtual Memory Systems&quot;">​</a></h1><p><a href="./Module-7">← Previous: Module 7</a> · <a href="./README">Subject index</a> · <a href="./Module-9">Next: Module 9 →</a></p><h2 id="learning-outcomes" tabindex="-1">Learning outcomes <a class="header-anchor" href="#learning-outcomes" aria-label="Permalink to &quot;Learning outcomes&quot;">​</a></h2><p>After this module, you should be able to:</p><ul><li>explain the concept and implementation of virtual memory;</li><li>describe the required hardware support (MMU, page tables, TLB);</li><li>explain the operating system&#39;s role and the value of memory abstraction.</li></ul><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><p>Complete <a href="./Module-7">Module 7</a> first. Review its quick-revision section if any term below feels unfamiliar.</p><h2 id="study-blocks" tabindex="-1">Study blocks <a class="header-anchor" href="#study-blocks" aria-label="Permalink to &quot;Study blocks&quot;">​</a></h2><p>Study one block at a time. Work through its example and checkpoint before continuing.</p><table tabindex="0"><thead><tr><th>Block</th><th>Topic</th><th>Suggested time</th></tr></thead><tbody><tr><td>1</td><td>Start here: the simple idea</td><td>10-15 minutes</td></tr><tr><td>2</td><td>Concept and implementation</td><td>10-15 minutes</td></tr><tr><td>3</td><td>Required hardware support</td><td>10-15 minutes</td></tr><tr><td>4</td><td>Operating system role in memory abstraction</td><td>10-15 minutes</td></tr></tbody></table><h2 id="start-here-the-simple-idea" tabindex="-1">Start here: the simple idea <a class="header-anchor" href="#start-here-the-simple-idea" aria-label="Permalink to &quot;Start here: the simple idea&quot;">​</a></h2><p><strong>Virtual memory</strong> gives programs each a large, private, contiguous-looking address space that is <strong>bigger</strong> than physical RAM by using the disk as an extension. The processor and OS cooperate to translate <strong>virtual addresses</strong> used by a program into <strong>physical addresses</strong> where data really lives.</p><h3 id="everyday-analogy" tabindex="-1">Everyday analogy <a class="header-anchor" href="#everyday-analogy" aria-label="Permalink to &quot;Everyday analogy&quot;">​</a></h3><p>Your computer&#39;s RAM is like a small desk you&#39;re actively working at. Virtual memory is like having a giant filing cabinet (the disk) plus an assistant (the MMU/OS) who fetches the file folders you ask for onto the desk, and stores others away when the desk gets full. To you, it feels as if the whole cabinet is on the desk.</p><h2 id="concept-and-implementation" tabindex="-1">Concept and implementation <a class="header-anchor" href="#concept-and-implementation" aria-label="Permalink to &quot;Concept and implementation&quot;">​</a></h2><h3 id="virtual-address-space-vs-physical-memory" tabindex="-1">Virtual address space vs physical memory <a class="header-anchor" href="#virtual-address-space-vs-physical-memory" aria-label="Permalink to &quot;Virtual address space vs physical memory&quot;">​</a></h3><ul><li>A program uses <strong>virtual addresses</strong> (e.g., 32-bit → 4 GiB addressable range).</li><li>Physical RAM is usually smaller than the <em>total</em> virtual space of all running programs, and a program may use more than physical RAM.</li><li>The OS divides both virtual and physical memory into equal-size <strong>pages</strong> (commonly 4 KiB).</li></ul><h3 id="paging" tabindex="-1">Paging <a class="header-anchor" href="#paging" aria-label="Permalink to &quot;Paging&quot;">​</a></h3><p>The OS keeps a <strong>page table</strong> per process: virtual page number → physical frame number, plus status bits. If a virtual page&#39;s physical frame holds another process&#39;s data, its entry says &quot;not present.&quot;</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Virtual address:  [ VPN ][ offset ]   (page offset, same in phys addr)</span></span>
<span class="line"><span>                  20 bits  12 bits    (4 KiB pages, 32-bit address)</span></span>
<span class="line"><span>Physical address: [ PFN ][ offset ]</span></span></code></pre></div><h3 id="the-page-fault-life-cycle" tabindex="-1">The page-fault life cycle <a class="header-anchor" href="#the-page-fault-life-cycle" aria-label="Permalink to &quot;The page-fault life cycle&quot;">​</a></h3><ol><li>Program accesses virtual page <code>V</code>.</li><li>MMU finds <code>V</code> marked <strong>not present</strong> (or protected) → raises a <strong>page fault</strong> (or segmentation fault).</li><li>The OS stops the program, picks a victim frame, writes it back to disk if dirty, reads the needed page from disk into the freed frame, updates the page table, and resumes the program.</li><li>The instruction restarts and now succeeds.</li></ol><h2 id="required-hardware-support" tabindex="-1">Required hardware support <a class="header-anchor" href="#required-hardware-support" aria-label="Permalink to &quot;Required hardware support&quot;">​</a></h2><p>Virtual memory requires hardware to translate fast:</p><h3 id="memory-management-unit-mmu" tabindex="-1">Memory Management Unit (MMU) <a class="header-anchor" href="#memory-management-unit-mmu" aria-label="Permalink to &quot;Memory Management Unit (MMU)&quot;">​</a></h3><ul><li>Translates every virtual address to a physical address on the fly, in <strong>every memory access</strong>.</li><li>Uses the page table (base register → page table in memory).</li></ul><h3 id="translation-lookaside-buffer-tlb" tabindex="-1">Translation Lookaside Buffer (TLB) <a class="header-anchor" href="#translation-lookaside-buffer-tlb" aria-label="Permalink to &quot;Translation Lookaside Buffer (TLB)&quot;">​</a></h3><ul><li>A small, fast <strong>cache of recent page-table translations</strong> (to avoid a memory access on every translation).</li><li>On a TLB <strong>hit</strong>, translation is fast.</li><li>On a TLB <strong>miss</strong>, the MMU walks the page table in memory, then fills the TLB.</li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Average memory access time (with TLB):</span></span>
<span class="line"><span>  TLB hit time:       t + t_cache</span></span>
<span class="line"><span>  TLB miss time:      t + t_cache + t_mem   (page-table walk)</span></span>
<span class="line"><span>  AMAT ≈ t + t_cache + (1 - hit_rate)·t_mem</span></span></code></pre></div><h3 id="protection-bits" tabindex="-1">Protection bits <a class="header-anchor" href="#protection-bits" aria-label="Permalink to &quot;Protection bits&quot;">​</a></h3><p>Each page-table entry carries:</p><ul><li><strong>Present/Absent (P):</strong> is the page in RAM or on disk?</li><li><strong>Read/Write/Execute (R/W/X):</strong> what accesses are allowed?</li><li><strong>User/Supervisor (U/S):</strong> can user-mode code access this page?</li><li><strong>Dirty bit:</strong> has the page been written (so it must be restored on eviction)?</li><li><strong>Accessed bit:</strong> has the page been read/written (used for replacement)?</li></ul><h3 id="address-width-registers" tabindex="-1">Address width registers <a class="header-anchor" href="#address-width-registers" aria-label="Permalink to &quot;Address width registers&quot;">​</a></h3><p>The OS must know how big a page is and where page tables live. x86 uses CR0–CR4 control registers and model-specific registers (MSRs); ARM uses <code>TTBR0_EL1</code> / <code>TCR_EL1</code> (translation table base / control).</p><h2 id="operating-system-role-in-memory-abstraction" tabindex="-1">Operating system role in memory abstraction <a class="header-anchor" href="#operating-system-role-in-memory-abstraction" aria-label="Permalink to &quot;Operating system role in memory abstraction&quot;">​</a></h2><p>The OS owns the policy; the hardware owns the mechanism.</p><h3 id="os-responsibilities" tabindex="-1">OS responsibilities <a class="header-anchor" href="#os-responsibilities" aria-label="Permalink to &quot;OS responsibilities&quot;">​</a></h3><ul><li><strong>Allocate pages</strong> to processes and set up page tables at <code>exec</code>/process start.</li><li><strong>Handle page faults:</strong> decide which page to evict (replacement policy).</li><li><strong>Page replacement:</strong> Least Recently Used (LRU) approximations are common; the OS uses the accessed/dirty bits to approximate LRU.</li><li><strong>Demand paging:</strong> load a page only when it is first accessed (lazy loading).</li><li><strong>Copy-on-write (COW):</strong> share parent/child memory after <code>fork</code>; copy a page only when one side writes — speeds up <code>fork</code>/<code>exec</code>.</li><li><strong>Shared memory:</strong> map the same physical frame into two processes&#39; address spaces.</li><li><strong>Protection:</strong> enforce read/write/execute/user permissions per page.</li></ul><h3 id="importance-of-abstraction-in-system-design" tabindex="-1">Importance of abstraction in system design <a class="header-anchor" href="#importance-of-abstraction-in-system-design" aria-label="Permalink to &quot;Importance of abstraction in system design&quot;">​</a></h3><p>Virtual memory is a textbook example of good abstraction:</p><ul><li><strong>Protection:</strong> processes can&#39;t read each other&#39;s or kernel memory.</li><li><strong>Simplification:</strong> programs use a simple linear address space; no need to fit everything in RAM manually.</li><li><strong>Sharing:</strong> map the same physical page into multiple address spaces (useful for shared libraries and IPC).</li><li><strong>Efficiency:</strong> run programs larger than RAM; keep the active set in fast RAM.</li><li><strong>Uniformity:</strong> memory-mapped files and devices look like ordinary memory.</li></ul><h2 id="common-mistakes" tabindex="-1">Common mistakes <a class="header-anchor" href="#common-mistakes" aria-label="Permalink to &quot;Common mistakes&quot;">​</a></h2><ul><li>Thinking virtual memory means &quot;more RAM&quot; — it trades disk for RAM (much slower).</li><li>Believing every unmapped access is a segmentation fault — a page fault is often normal.</li><li>Ignoring the TLB: software that thrashes the TLB/page tables runs slowly.</li></ul><h2 id="memory-rules" tabindex="-1">Memory rules <a class="header-anchor" href="#memory-rules" aria-label="Permalink to &quot;Memory rules&quot;">​</a></h2><ul><li>Virtual memory gives each process a big private address space using disk.</li><li>Pages: virtual page → physical frame; offsets are unchanged.</li><li>MMU translates; TLB caches translations.</li><li>Page fault = OS loads the page from disk (demand paging).</li><li>Protection bits: P, R/W/X, U/S, dirty, accessed.</li><li>Abstraction value: protection, sharing, simple addressing, demand paging.</li></ul><h2 id="check-your-understanding" tabindex="-1">Check your understanding <a class="header-anchor" href="#check-your-understanding" aria-label="Permalink to &quot;Check your understanding&quot;">​</a></h2><ol><li>In one line, what does virtual memory add beyond physical RAM?</li><li>What is the difference between a page fault and a segmentation fault?</li><li>What is the TLB and why is it needed?</li><li>What OS policy does the hardware mechanism enable for virtual memory?</li><li>Give one benefit of copy-on-write.</li></ol><h2 id="answers" tabindex="-1">Answers <a class="header-anchor" href="#answers" aria-label="Permalink to &quot;Answers&quot;">​</a></h2><details><summary>Reveal answers after attempting the questions</summary><ol><li>It lets a process use an address space larger than RAM by storing inactive pages on disk and loading them on demand.</li><li>A page fault is a normal, recoverable event (page not in RAM → load it); a segmentation fault is an invalid, unrecoverable access (bad address, or protection violation).</li><li>The TLB is a cache of recent virtual→physical translations; without it, every memory access would cost an extra page-table walk.</li><li>The OS decides page allocation, replacement, demand paging, and protection policies; the MMU/TLB perform fast translation and fault generation.</li><li>It allows <code>fork</code> to share pages between parent and child, copying only on write, so <code>fork</code>+<code>exec</code> is fast and uses little memory until a write.</li></ol></details><h2 id="quick-revision-box" tabindex="-1">Quick revision box <a class="header-anchor" href="#quick-revision-box" aria-label="Permalink to &quot;Quick revision box&quot;">​</a></h2><ul><li>Virtual memory: each process gets a big private virtual address space.</li><li>Paging: VFN→PFN mapping via page tables; offsets unchanged.</li><li>MMU translates; TLB caches translations; page tables in memory.</li><li>Page fault: OS loads page from disk (demand paging); replacement (LRU approx).</li><li>Protection bits P/R-W-X, U/S, dirty, accessed.</li><li>Abstraction benefits: protection, sharing, simplicity, demand paging.</li></ul><h2 id="exam-guidance" tabindex="-1">Exam guidance <a class="header-anchor" href="#exam-guidance" aria-label="Permalink to &quot;Exam guidance&quot;">​</a></h2><p>Draw the relevant block or timing diagram, label data movement, show the calculation, and explain the performance consequence.</p><h2 id="practice-ladder" tabindex="-1">Practice ladder <a class="header-anchor" href="#practice-ladder" aria-label="Permalink to &quot;Practice ladder&quot;">​</a></h2><ol><li><strong>Easy - Recall:</strong> Define the module&#39;s central idea in one or two sentences.</li><li><strong>Easy - Recognize:</strong> Identify the correct method for a small example and explain why it fits.</li><li><strong>Medium - Apply:</strong> Work through one representative problem without copying the example.</li><li><strong>Medium - Compare:</strong> Contrast two methods or concepts from the module.</li><li><strong>Hard - Integrate:</strong> Solve a university-style scenario and justify every major step.</li></ol><details><summary>Reveal self-evaluation guide</summary><p>A complete response uses correct terminology, shows intermediate steps, connects the result to the scenario, and states one assumption or limitation.</p></details><hr><p><a href="./Module-7">← Previous: Module 7</a> · <a href="./README">Subject index</a> · <a href="./Module-9">Next: Module 9 →</a></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Module-8.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module8 = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module8 as default
};
