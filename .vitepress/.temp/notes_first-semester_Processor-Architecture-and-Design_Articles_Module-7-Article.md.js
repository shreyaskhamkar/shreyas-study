import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 7 Article: Virtualization and the Illusion of Dedicated Hardware","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-7-Article.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-7-Article.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Articles/Module-7-Article.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-7-article-virtualization-and-the-illusion-of-dedicated-hardware" tabindex="-1">Module 7 Article: Virtualization and the Illusion of Dedicated Hardware <a class="header-anchor" href="#module-7-article-virtualization-and-the-illusion-of-dedicated-hardware" aria-label="Permalink to &quot;Module 7 Article: Virtualization and the Illusion of Dedicated Hardware&quot;">​</a></h1><h2 id="the-big-idea" tabindex="-1">The big idea <a class="header-anchor" href="#the-big-idea" aria-label="Permalink to &quot;The big idea&quot;">​</a></h2><p>Multiple programs run on one computer, yet each behaves as though it <strong>owns</strong> the CPU, the memory, and the devices. This <strong>illusion of dedicated hardware</strong> is the central trick of modern computing — and it is achieved purely by software abstraction layers backed by a little hardware support.</p><h2 id="the-three-illusions-and-how-each-is-built" tabindex="-1">The three illusions and how each is built <a class="header-anchor" href="#the-three-illusions-and-how-each-is-built" aria-label="Permalink to &quot;The three illusions and how each is built&quot;">​</a></h2><h3 id="_1-cpu-illusion-time-slicing" tabindex="-1">1. CPU illusion: time-slicing <a class="header-anchor" href="#_1-cpu-illusion-time-slicing" aria-label="Permalink to &quot;1. CPU illusion: time-slicing&quot;">​</a></h3><p>A single core can run only one program at a time, but the OS switches between programs every <strong>time slice</strong> (a few milliseconds). Each program sees the CPU almost continuously. Two classic forms:</p><ul><li><strong>Multiprogramming:</strong> when a program blocks on I/O, switch to another.</li><li><strong>Time sharing:</strong> preempt with a <strong>timer interrupt</strong> to keep all programs responsive.</li></ul><h3 id="_2-memory-illusion-virtual-address-spaces" tabindex="-1">2. Memory illusion: virtual address spaces <a class="header-anchor" href="#_2-memory-illusion-virtual-address-spaces" aria-label="Permalink to &quot;2. Memory illusion: virtual address spaces&quot;">​</a></h3><p>Every process uses a <strong>virtual address space</strong> and thinks memory starts at a fixed low address and is all its own. The OS + <strong>MMU</strong> translate virtual → physical (Module 8). Benefits:</p><ul><li><strong>Isolation:</strong> one process cannot read or write another&#39;s memory.</li><li><strong>Overcommit:</strong> total virtual memory can exceed physical RAM.</li><li><strong>Simplified code:</strong> programs assume a flat, contiguous address space.</li></ul><h3 id="_3-i-o-illusion-files-and-drivers" tabindex="-1">3. I/O illusion: files and drivers <a class="header-anchor" href="#_3-i-o-illusion-files-and-drivers" aria-label="Permalink to &quot;3. I/O illusion: files and drivers&quot;">​</a></h3><p>Devices appear as ordinary <strong>files</strong> (<code>/dev/sda</code>, <code>COM1</code>, a TCP socket). The program reads/writes as if it owned the device; the OS <strong>driver</strong> and the device controller actually multiplex the physical device among all processes.</p><h2 id="virtualization-selling-whole-machines" tabindex="-1">Virtualization: selling whole machines <a class="header-anchor" href="#virtualization-selling-whole-machines" aria-label="Permalink to &quot;Virtualization: selling whole machines&quot;">​</a></h2><p><strong>Virtualization</strong> wraps an entire machine and presents a <strong>virtual machine (VM)</strong> to a guest OS, as if the guest owned dedicated hardware. A thin software layer — the <strong>hypervisor</strong> — does the multiplexing.</p><ul><li><strong>Type 1 (bare-metal):</strong> runs straight on the hardware (VMware ESXi, Xen, Microsoft Hyper-V). Higher performance, used in servers.</li><li><strong>Type 2 (hosted):</strong> runs as an application on a host OS (VirtualBox, VMware Workstation, Parallels). Convenient, slightly slower.</li></ul><h2 id="memory-virtualization-needs-extra-translation" tabindex="-1">Memory virtualization needs extra translation <a class="header-anchor" href="#memory-virtualization-needs-extra-translation" aria-label="Permalink to &quot;Memory virtualization needs extra translation&quot;">​</a></h2><p>Per-process virtual memory is already guest-virtual → host-physical. To run a VM, the hypervisor adds a <strong>second translation</strong>: guest-virtual → guest-physical → host-physical. Modern CPUs provide <strong>nested paging</strong> (Intel EPT / AMD RVI / ARM Stage 2) to do both translations in hardware, so the overhead is tolerable.</p><h2 id="i-o-virtualization" tabindex="-1">I/O virtualization <a class="header-anchor" href="#i-o-virtualization" aria-label="Permalink to &quot;I/O virtualization&quot;">​</a></h2><ul><li><strong>Emulation:</strong> the hypervisor fakes a complete device in software. Portable but slow (e.g., an emulated Intel e1000 NIC).</li><li><strong>Para-virtualization:</strong> the guest knows it is virtualized and calls the hypervisor directly (virtio). Much faster.</li><li><strong>Device assignment (VT-d/IOMMU):</strong> the hypervisor maps a real device directly into a VM using an <strong>I/O memory-management unit</strong> for protection.</li></ul><h2 id="worked-example-why-isolation-matters" tabindex="-1">Worked example: why isolation matters <a class="header-anchor" href="#worked-example-why-isolation-matters" aria-label="Permalink to &quot;Worked example: why isolation matters&quot;">​</a></h2><p>Process A (browser) and Process B (banking app) both <code>malloc</code> and write memory.</p><ul><li>Without protection: B could read A&#39;s decrypted data via a stray pointer.</li><li>With protection rings + virtual memory: A&#39;s pages are marked for A&#39;s address space only; any access by B raises a segmentation fault.</li></ul><h2 id="exam-angle" tabindex="-1">Exam angle <a class="header-anchor" href="#exam-angle" aria-label="Permalink to &quot;Exam angle&quot;">​</a></h2><p>For an isolation/virtualization question:</p><ol><li>State the <strong>illusion</strong> (dedicated hardware) and why it matters (multipurpose, safe, efficient).</li><li>Name each illusion and the mechanism: CPU (time-slicing + timer), memory (page tables + MMU), I/O (files + drivers).</li><li>Add virtualization: hypervisor (Type 1/2), nested paging, emulated vs para-virtualized I/O.</li><li>Close with the trade-off: convenience and safety vs. overhead.</li></ol><blockquote><p>Tip: distinguish <strong>multiprogramming</strong> (throughput) from <strong>time sharing</strong> (interactive response) — examiners like the contrast.</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Articles/Module-7-Article.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module7Article = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module7Article as default
};
