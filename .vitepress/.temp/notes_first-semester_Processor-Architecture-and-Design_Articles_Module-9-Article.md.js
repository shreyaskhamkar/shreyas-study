import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 9 Article: Interrupts, Exceptions, and the ISR Lifecycle","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-9-Article.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-9-Article.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Articles/Module-9-Article.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-9-article-interrupts-exceptions-and-the-isr-lifecycle" tabindex="-1">Module 9 Article: Interrupts, Exceptions, and the ISR Lifecycle <a class="header-anchor" href="#module-9-article-interrupts-exceptions-and-the-isr-lifecycle" aria-label="Permalink to &quot;Module 9 Article: Interrupts, Exceptions, and the ISR Lifecycle&quot;">​</a></h1><h2 id="the-big-idea" tabindex="-1">The big idea <a class="header-anchor" href="#the-big-idea" aria-label="Permalink to &quot;The big idea&quot;">​</a></h2><p>The CPU cannot poll every device forever — that wastes cycles. Instead, devices <strong>ask for attention</strong> with an <strong>interrupt</strong>; the CPU pauses, handles the event in a small handler, and resumes. This single mechanism is behind responsive keyboards, disk I/O that doesn&#39;t freeze the screen, and the timer that lets multitasking exist.</p><h2 id="three-kinds-of-control-transfer-events" tabindex="-1">Three kinds of control-transfer events <a class="header-anchor" href="#three-kinds-of-control-transfer-events" aria-label="Permalink to &quot;Three kinds of control-transfer events&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Kind</th><th>When</th><th>Who triggers</th><th>Example</th></tr></thead><tbody><tr><td>Exception</td><td>During instruction execution</td><td>CPU/hw internally</td><td>divide-by-zero, page fault</td></tr><tr><td>Interrupt</td><td>Anytime, asynchronously</td><td>external device</td><td>keyboard, disk done</td></tr><tr><td>Trap</td><td>When program calls</td><td>the program</td><td>system call (<code>syscall</code>)</td></tr></tbody></table><p>A <strong>trap</strong> is really just a synchronous, deliberate interrupt used to enter the OS. The hardware that handles a hardware interrupt handles a system call too.</p><h2 id="the-full-lifecycle-of-an-interrupt" tabindex="-1">The full lifecycle of an interrupt <a class="header-anchor" href="#the-full-lifecycle-of-an-interrupt" aria-label="Permalink to &quot;The full lifecycle of an interrupt&quot;">​</a></h2><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">flowchart TD</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    A[Running user code] --&gt; B{Interrupt/Exception raised?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    B --&gt;|No| C[Continue user code]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    B --&gt;|Yes| D[Finish current instruction]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    D --&gt; E[Save PC + flags to kernel stack]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    E --&gt; F[Switch to kernel/supervisor mode]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    F --&gt; G[Read cause register / vector]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    G --&gt; H[Jump to the ISR (via vector table)]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    H --&gt; I[ISR: clear source, do work, maybe wake a process]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    I --&gt; J{Called from user mode?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    J --&gt;|Yes| K[Restore user state and return to next instruction]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    J --&gt;|No| L[Schedule / return to kernel]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    K --&gt; C</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    L --&gt; C</span></span></code></pre></div><p>Key details:</p><ul><li>The CPU <strong>automatically</strong> saves the return address and processor status so the program can resume exactly where it stopped.</li><li>A <strong>dedicated kernel stack</strong> is swapped in, because the user stack is not trusted.</li><li>The <strong>interrupt vector</strong> is a table of handler addresses. The CPU indexes it with the interrupt number.</li></ul><h2 id="masking-priorities-and-nesting" tabindex="-1">Masking, priorities, and nesting <a class="header-anchor" href="#masking-priorities-and-nesting" aria-label="Permalink to &quot;Masking, priorities, and nesting&quot;">​</a></h2><ul><li><strong>Maskable (IRQ):</strong> can be disabled by clearing the interrupt-enable flag (<code>cli</code>/<code>sti</code> on x86, <code>msr daif, #0xf</code> on ARM). Used for short critical sections.</li><li><strong>Non-maskable (NMI):</strong> cannot be disabled; used for critical events (power failure, watchdog).</li><li><strong>Priority + nesting:</strong> a higher-priority interrupt can preempt a running ISR. The CPU pushes another frame; this is safe only if ISRs are <strong>reentrant</strong>.</li></ul><h2 id="the-interrupt-vector-table" tabindex="-1">The interrupt vector table <a class="header-anchor" href="#the-interrupt-vector-table" aria-label="Permalink to &quot;The interrupt vector table&quot;">​</a></h2><p>Each interrupt source is assigned a small number (the vector). The CPU multiplies that by the word size and adds it to a base register (e.g. x86 <code>IDTR</code>, ARM <code>VBAR</code>/<code>VBR</code>) to find the handler&#39;s address:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>VectorBase + vector_number × entry_size → handler_address</span></span></code></pre></div><p>The OS fills this table at boot (in C code that installs function pointers), linking a device IRQ to the right ISR.</p><h2 id="writing-and-managing-isrs" tabindex="-1">Writing and managing ISRs <a class="header-anchor" href="#writing-and-managing-isrs" aria-label="Permalink to &quot;Writing and managing ISRs&quot;">​</a></h2><p>An ISR is a function with special rules:</p><ol><li><strong>Save</strong> any registers it will clobber (or use a dedicated IRQ register set).</li><li><strong>Identify the source</strong> — read the device&#39;s interrupt-status register.</li><li><strong>Handle it</strong> — clear the interrupt flag at the device (critical: if you don&#39;t, the interrupt fires again immediately).</li><li><strong>Do the minimum</strong> — copy data to a queue and defer heavy work to a <strong>top half</strong> / tasklet / softirq, or wake a waiting process via the OS scheduler.</li><li><strong>Return</strong> with the special instruction that restores saved state (<code>iret</code> on x86, <code>eret</code> on ARM) and drops back to user mode.</li></ol><h3 id="the-always-clear-the-source-rule" tabindex="-1">The &quot;always clear the source&quot; rule <a class="header-anchor" href="#the-always-clear-the-source-rule" aria-label="Permalink to &quot;The &quot;always clear the source&quot; rule&quot;">​</a></h3><p>If an ISR doesn&#39;t clear the device&#39;s interrupt flag, the device keeps interrupting — creating an <strong>interrupt storm</strong> that locks up the machine. This is the single most common ISR bug.</p><h2 id="system-calls-use-the-same-path" tabindex="-1">System calls use the same path <a class="header-anchor" href="#system-calls-use-the-same-path" aria-label="Permalink to &quot;System calls use the same path&quot;">​</a></h2><p>A program that calls <code>write(fd, buf, n)</code> does roughly:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">mov  rax, SYS_write</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     // syscall number</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">mov  rdi, fd</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">mov  rsi, buf</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">mov  rdx, n</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">syscall</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                 // trap into kernel</span></span></code></pre></div><p>The CPU switches to kernel mode, jumps to the syscall entry, the OS dispatches <code>sys_write</code>, copies data to the file, and returns the byte count. Same hardware, deliberate trigger.</p><h2 id="worked-example-a-timer-isr" tabindex="-1">Worked example: a timer ISR <a class="header-anchor" href="#worked-example-a-timer-isr" aria-label="Permalink to &quot;Worked example: a timer ISR&quot;">​</a></h2><p>The kernel timer fires every 1 ms (preemption tick):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ISR(timer):</span></span>
<span class="line"><span>    save regs</span></span>
<span class="line"><span>    increment ticks</span></span>
<span class="line"><span>    if current process timeslice expired:</span></span>
<span class="line"><span>        set need_resched = 1</span></span>
<span class="line"><span>        wake scheduler</span></span>
<span class="line"><span>    clear timer flag          # MUST DO</span></span>
<span class="line"><span>    restore regs</span></span>
<span class="line"><span>    iret</span></span></code></pre></div><p>The preemption flag tells the OS to switch processes at the next opportunity.</p><h2 id="exam-angle" tabindex="-1">Exam angle <a class="header-anchor" href="#exam-angle" aria-label="Permalink to &quot;Exam angle&quot;">​</a></h2><p>For &quot;explain how an interrupt is handled&quot;:</p><ol><li>Device raises IRQ line.</li><li>CPU finishes the current instruction.</li><li>Save PC + flags; switch to kernel mode and a kernel stack.</li><li>Index the interrupt vector; jump to the ISR.</li><li>ISR clears the source, does minimal work (or schedules deferred work).</li><li>Restore state and return to the interrupted instruction.</li></ol><blockquote><p>Tip: emphasize (a) switching to <strong>kernel mode</strong>, (b) <strong>saving state</strong>, and (c) <strong>clearing the interrupt source</strong> — these three points carry most of the marks.</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Articles/Module-9-Article.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module9Article = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module9Article as default
};
