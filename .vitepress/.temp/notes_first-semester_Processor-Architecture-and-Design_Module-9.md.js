import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 9: Interrupts and Exception Handling","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Module-9.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Module-9.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Module-9.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-9-interrupts-and-exception-handling" tabindex="-1">Module 9: Interrupts and Exception Handling <a class="header-anchor" href="#module-9-interrupts-and-exception-handling" aria-label="Permalink to &quot;Module 9: Interrupts and Exception Handling&quot;">​</a></h1><p><a href="./Module-8">← Previous: Module 8</a> · <a href="./README">Subject index</a> · <a href="./Module-10">Next: Module 10 →</a></p><h2 id="learning-outcomes" tabindex="-1">Learning outcomes <a class="header-anchor" href="#learning-outcomes" aria-label="Permalink to &quot;Learning outcomes&quot;">​</a></h2><p>After this module, you should be able to:</p><ul><li>distinguish traps, exceptions, and interrupts;</li><li>explain user mode and kernel mode;</li><li>describe how a computer responds to interrupts and events;</li><li>relate interrupts to system calls;</li><li>write and manage interrupt service routines (ISRs).</li></ul><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><p>Complete <a href="./Module-8">Module 8</a> first. Review its quick-revision section if any term below feels unfamiliar.</p><h2 id="study-blocks" tabindex="-1">Study blocks <a class="header-anchor" href="#study-blocks" aria-label="Permalink to &quot;Study blocks&quot;">​</a></h2><p>Study one block at a time. Work through its example and checkpoint before continuing.</p><table tabindex="0"><thead><tr><th>Block</th><th>Topic</th><th>Suggested time</th></tr></thead><tbody><tr><td>1</td><td>Start here: the simple idea</td><td>10-15 minutes</td></tr><tr><td>2</td><td>Types: Traps, Exceptions, Interrupts</td><td>10-15 minutes</td></tr><tr><td>3</td><td>Handling non-deterministic events</td><td>10-15 minutes</td></tr><tr><td>4</td><td>User mode and kernel mode</td><td>10-15 minutes</td></tr><tr><td>5</td><td>How computers handle interrupts and events</td><td>10-15 minutes</td></tr><tr><td>6</td><td>Interrupts and system calls</td><td>10-15 minutes</td></tr><tr><td>7</td><td>Responding to the unexpected</td><td>10-15 minutes</td></tr><tr><td>8</td><td>Writing and managing interrupt service routines (ISRs)</td><td>10-15 minutes</td></tr></tbody></table><h2 id="start-here-the-simple-idea" tabindex="-1">Start here: the simple idea <a class="header-anchor" href="#start-here-the-simple-idea" aria-label="Permalink to &quot;Start here: the simple idea&quot;">​</a></h2><p>An <strong>interrupt</strong> is an asynchronous (or semi-synchronous) signal that says &quot;stop what you&#39;re doing and handle this now.&quot; The CPU pauses, remembers enough to resume, runs a small handler, and resumes. This lets the CPU wait for slow devices (keyboard, disk) instead of busy-waiting.</p><h3 id="everyday-analogy" tabindex="-1">Everyday analogy <a class="header-anchor" href="#everyday-analogy" aria-label="Permalink to &quot;Everyday analogy&quot;">​</a></h3><p>The CPU is a chef cooking a meal. A <strong>timer interrupt</strong> is the kitchen timer ringing — the chef checks the clock and moves on. A <strong>keyboard interrupt</strong> is a customer tapping the chef&#39;s shoulder to place an order. An <strong>exception</strong> is the chef noticing the pan is smoking and switching to put it out immediately.</p><h2 id="types-traps-exceptions-interrupts" tabindex="-1">Types: Traps, Exceptions, Interrupts <a class="header-anchor" href="#types-traps-exceptions-interrupts" aria-label="Permalink to &quot;Types: Traps, Exceptions, Interrupts&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Type</th><th>Trigger</th><th>Synchronous?</th><th>Who raises it</th></tr></thead><tbody><tr><td><strong>Exception</strong></td><td>Instruction error (divide-by-zero, page fault, invalid opcode)</td><td>Yes</td><td>hardware/OS while executing</td></tr><tr><td><strong>Interrupt</strong></td><td>External device (keyboard, disk, timer)</td><td>No</td><td>I/O device/hardware</td></tr><tr><td><strong>Trap</strong></td><td>Software request (system call <code>syscall</code>/<code>int</code>)</td><td>Yes</td><td>the program itself</td></tr></tbody></table><h3 id="exceptions-synchronous" tabindex="-1">Exceptions (synchronous) <a class="header-anchor" href="#exceptions-synchronous" aria-label="Permalink to &quot;Exceptions (synchronous)&quot;">​</a></h3><p>Generated by <strong>executing a specific instruction</strong>:</p><ul><li><strong>Fault:</strong> recoverable, the faulting instruction is restarted after the handler (e.g. a page fault that succeeds in loading the page).</li><li><strong>Trap:</strong> reported <em>after</em> the instruction completes, then control returns to the next instruction (e.g. a system call via <code>int</code>/<code>svc</code>).</li><li><strong>Abort:</strong> unrecoverable; the program is terminated (e.g. a segmentation fault on an invalid address that cannot be loaded).</li></ul><h3 id="interrupts-asynchronous" tabindex="-1">Interrupts (asynchronous) <a class="header-anchor" href="#interrupts-asynchronous" aria-label="Permalink to &quot;Interrupts (asynchronous)&quot;">​</a></h3><p>Generated <strong>independently of the current instruction</strong> by hardware:</p><ul><li><strong>Maskable (IRQ):</strong> can be ignored/frozen for a while; the CPU checks a flag between instructions.</li><li><strong>Non-maskable (NMI):</strong> cannot be ignored; used for critical events (power loss).</li><li>Edge-triggered vs level-triggered signalling on the pin.</li></ul><h3 id="the-interrupt-vector" tabindex="-1">The interrupt vector <a class="header-anchor" href="#the-interrupt-vector" aria-label="Permalink to &quot;The interrupt vector&quot;">​</a></h3><p>Each interrupt/exception is assigned a <strong>vector</strong>: an index into an <strong>interrupt vector table</strong> that points to the handler address. The CPU saves the current state and jumps to the right handler.</p><h2 id="handling-non-deterministic-events" tabindex="-1">Handling non-deterministic events <a class="header-anchor" href="#handling-non-deterministic-events" aria-label="Permalink to &quot;Handling non-deterministic events&quot;">​</a></h2><p>Non-deterministic = unpredictable <em>when</em> they arrive (e.g. a key press). The design must be robust:</p><ol><li><strong>Detect</strong> via a hardware flag/pin.</li><li><strong>Interrupt acknowledge</strong>: CPU finishes the current instruction, saves program status (PC, flags), and switches to <strong>kernel/supervisor mode</strong>.</li><li><strong>Dispatch</strong>: index the vector table, jump to the <strong>ISR</strong> (interrupt service routine).</li><li><strong>Serve</strong>: the ISR reads the device, does the work (often briefly), and may wake a waiting process.</li><li><strong>Return</strong>: restore the saved state and resume the interrupted program at the next instruction.</li></ol><h3 id="nested-and-prioritized-interrupts" tabindex="-1">Nested and prioritized interrupts <a class="header-anchor" href="#nested-and-prioritized-interrupts" aria-label="Permalink to &quot;Nested and prioritized interrupts&quot;">​</a></h3><ul><li><strong>Nesting:</strong> while handling an ISR, a higher-priority interrupt can preempt it; the CPU stacks more state.</li><li><strong>Priority:</strong> devices are prioritized so urgent events (power fail) preempt routine ones (network packet).</li></ul><h2 id="user-mode-and-kernel-mode" tabindex="-1">User mode and kernel mode <a class="header-anchor" href="#user-mode-and-kernel-mode" aria-label="Permalink to &quot;User mode and kernel mode&quot;">​</a></h2><p>Modern CPUs have <strong>protection rings</strong> (x86: 0–3; ARM: EL0/EL1):</p><ul><li><strong>User mode (ring 3 / EL0):</strong> limited; cannot execute I/O or privileged instructions. A privileged instruction or bad address raises an exception.</li><li><strong>Kernel/supervisor mode (ring 0 / EL1):</strong> full access to memory, I/O, and all instructions.</li></ul><p>This separation protects the OS: a buggy user program cannot directly touch hardware.</p><h2 id="how-computers-handle-interrupts-and-events" tabindex="-1">How computers handle interrupts and events <a class="header-anchor" href="#how-computers-handle-interrupts-and-events" aria-label="Permalink to &quot;How computers handle interrupts and events&quot;">​</a></h2><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">flowchart TD</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    A[Running user code] --&gt; B{Interrupt/Exception/Trap?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    B --&gt;|Yes| C[Finish current instr; save PC/flags; switch to kernel mode]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    C --&gt; D[Read interrupt vector / cause register]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    D --&gt; E[Jump to ISR]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    E --&gt; F[ISR: handle device / service / fix]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    F --&gt; G{Device needs retry or process wakeup?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    G --&gt;|Yes| H[Schedule; possibly sleep caller]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    G --&gt;|No| I</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    H --&gt; I[Restore saved PC/flags; switch back to user mode]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    B --&gt;|No| J[Continue user code]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    I --&gt; J</span></span></code></pre></div><h2 id="interrupts-and-system-calls" tabindex="-1">Interrupts and system calls <a class="header-anchor" href="#interrupts-and-system-calls" aria-label="Permalink to &quot;Interrupts and system calls&quot;">​</a></h2><p>A <strong>system call</strong> is the program&#39;s polite way of requesting OS service. On x86: <code>int 0x80</code>, or <code>syscall</code>/<code>sysenter</code>; on ARM: <code>svc #0</code>. This triggers a <strong>synchronous trap</strong>:</p><ol><li>Program prepares arguments (registers/stack).</li><li>Executes the syscall instruction.</li><li>CPU switches to kernel mode and jumps to the syscall handler (an OS entry).</li><li>The OS dispatches to the requested service (e.g., <code>read</code>, <code>write</code>, <code>fork</code>).</li><li>The OS returns the result and <strong>returns to user mode</strong>.</li></ol><p>Note: a system call uses the same trap/exception hardware as faults — the difference is <em>who</em> raised it (the program vs. the hardware).</p><h2 id="responding-to-the-unexpected" tabindex="-1">Responding to the unexpected <a class="header-anchor" href="#responding-to-the-unexpected" aria-label="Permalink to &quot;Responding to the unexpected&quot;">​</a></h2><ul><li><strong>Polling</strong> (busy-wait): CPU repeatedly checks a device flag = wastes CPU.</li><li><strong>Interrupt-driven I/O:</strong> device raises an interrupt when ready; CPU does useful work meanwhile. Much better for slow, unpredictable devices.</li><li><strong>DMA (Direct Memory Access):</strong> a controller transfers a whole block between device and memory without CPU intervention, then raises an interrupt when done.</li></ul><h2 id="writing-and-managing-interrupt-service-routines-isrs" tabindex="-1">Writing and managing interrupt service routines (ISRs) <a class="header-anchor" href="#writing-and-managing-interrupt-service-routines-isrs" aria-label="Permalink to &quot;Writing and managing interrupt service routines (ISRs)&quot;">​</a></h2><p>An ISR must be:</p><ul><li><strong>Fast:</strong> do the minimum (e.g., read status, clear the interrupt source, queue data for later processing in a <strong>top half</strong> / deferred procedure call).</li><li><strong>Reentrant / safe:</strong> it can interrupt normal code and even itself; avoid blocking, avoid non-reentrant functions.</li><li><strong>Correct about sources:</strong> always clear the device&#39;s interrupt flag, or it will re-trigger immediately.</li></ul><p><strong>Common ISR structure:</strong></p><ol><li>Save registers the ISR will clobber (or use a dedicated IRQ stack).</li><li>Identify the device/cause.</li><li>Handle it (acknowledge, copy data, update state, wake a process).</li><li>Clear the interrupt source.</li><li>Restore saved state and return via an interrupt-return instruction (e.g., <code>iret</code> on x86, <code>eret</code> on ARM).</li></ol><p><strong>Managing ISRs:</strong> register the handler in the vector table; mask/unmask the IRQ line; set priorities; keep the top half (deferred work) separate.</p><h2 id="common-mistakes" tabindex="-1">Common mistakes <a class="header-anchor" href="#common-mistakes" aria-label="Permalink to &quot;Common mistakes&quot;">​</a></h2><ul><li>Doing long work inside the ISR (should defer to a tasklet/softirq).</li><li>Forgetting to clear the device&#39;s interrupt flag (causes an interrupt storm).</li><li>Using non-reentrant code or blocking inside an ISR.</li><li>Confusing a synchronous trap (syscall) with an asynchronous hardware interrupt.</li></ul><h2 id="memory-rules" tabindex="-1">Memory rules <a class="header-anchor" href="#memory-rules" aria-label="Permalink to &quot;Memory rules&quot;">​</a></h2><ul><li>Exception: synchronous (instruction causes it), trap (post-completion), abort (fatal).</li><li>Interrupt: asynchronous (external device); maskable vs non-maskable.</li><li>User mode = restricted; kernel mode = full access.</li><li>ISR = detect → save state → dispatch → serve → restore → return.</li><li>System call = program-raised synchronous trap into the OS.</li></ul><h2 id="check-your-understanding" tabindex="-1">Check your understanding <a class="header-anchor" href="#check-your-understanding" aria-label="Permalink to &quot;Check your understanding&quot;">​</a></h2><ol><li>Which of the three types (exception/interrupt/trap) is synchronous, and which is asynchronous?</li><li>What is the difference between a fault and an abort?</li><li>Why switch to kernel mode before running an ISR?</li><li>How does a system call use the same hardware as a hardware interrupt?</li><li>Give two rules for writing ISRs.</li></ol><h2 id="answers" tabindex="-1">Answers <a class="header-anchor" href="#answers" aria-label="Permalink to &quot;Answers&quot;">​</a></h2><details><summary>Reveal answers after attempting the questions</summary><ol><li>Trap and exception are synchronous (caused by/aware of the current instruction); hardware interrupts are asynchronous.</li><li>A fault is recoverable and restarts the offending instruction; an abort is unrecoverable and terminates the program.</li><li>To prevent the (possibly untrusted or buggy) user program from directly touching hardware/I/O and to keep the OS kernel protected.</li><li>Both save the current state, switch to kernel mode, index a vector table, and jump to a handler; a syscall is simply a trap deliberately raised by the program.</li><li>Keep ISRs fast and defer heavy work; always clear the device&#39;s interrupt flag, and avoid blocking/non-reentrant calls.</li></ol></details><h2 id="quick-revision-box" tabindex="-1">Quick revision box <a class="header-anchor" href="#quick-revision-box" aria-label="Permalink to &quot;Quick revision box&quot;">​</a></h2><ul><li>Exception (synchronous, incl. page fault/segfault) vs interrupt (async, device) vs trap (syscall).</li><li>Faults recover &amp; restart; aborts kill the program.</li><li>User mode = restricted; kernel mode = full privileges.</li><li>ISR flow: save state → dispatch → serve → clear source → restore → return.</li><li>System call = software trap into the OS; same vector/hw mechanism as interrupts.</li><li>ISRs must be short, reentrant, and always clear the interrupt source.</li></ul><h2 id="exam-guidance" tabindex="-1">Exam guidance <a class="header-anchor" href="#exam-guidance" aria-label="Permalink to &quot;Exam guidance&quot;">​</a></h2><p>Draw the relevant block or timing diagram, label data movement, show the calculation, and explain the performance consequence.</p><h2 id="practice-ladder" tabindex="-1">Practice ladder <a class="header-anchor" href="#practice-ladder" aria-label="Permalink to &quot;Practice ladder&quot;">​</a></h2><ol><li><strong>Easy - Recall:</strong> Define the module&#39;s central idea in one or two sentences.</li><li><strong>Easy - Recognize:</strong> Identify the correct method for a small example and explain why it fits.</li><li><strong>Medium - Apply:</strong> Work through one representative problem without copying the example.</li><li><strong>Medium - Compare:</strong> Contrast two methods or concepts from the module.</li><li><strong>Hard - Integrate:</strong> Solve a university-style scenario and justify every major step.</li></ol><details><summary>Reveal self-evaluation guide</summary><p>A complete response uses correct terminology, shows intermediate steps, connects the result to the scenario, and states one assumption or limitation.</p></details><hr><p><a href="./Module-8">← Previous: Module 8</a> · <a href="./README">Subject index</a> · <a href="./Module-10">Next: Module 10 →</a></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Module-9.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module9 = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module9 as default
};
