import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 10 Article: API, ABI, and the System Call Boundary","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-10-Article.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-10-Article.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Articles/Module-10-Article.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-10-article-api-abi-and-the-system-call-boundary" tabindex="-1">Module 10 Article: API, ABI, and the System Call Boundary <a class="header-anchor" href="#module-10-article-api-abi-and-the-system-call-boundary" aria-label="Permalink to &quot;Module 10 Article: API, ABI, and the System Call Boundary&quot;">​</a></h1><h2 id="the-big-idea" tabindex="-1">The big idea <a class="header-anchor" href="#the-big-idea" aria-label="Permalink to &quot;The big idea&quot;">​</a></h2><p>For code to talk to an OS or another library without rewriting everything each time, it needs a stable <strong>interface</strong>. Two layers matter:</p><ul><li>An <strong>API</strong> (application programming interface): the <em>source-level</em> contract — function names, argument types, and headers.</li><li>An <strong>ABI</strong> (application binary interface): the <em>binary</em> contract — calling convention, name mangling, struct layout, and library format.</li></ul><p>A program is source-compatible if it recompiles against a new version; it is binary-compatible if the <strong>existing executable</strong> keeps running without recompilation. ABIs are harder to keep stable, and that is where compatibility breaks.</p><h2 id="api-vs-abi-concretely" tabindex="-1">API vs ABI, concretely <a class="header-anchor" href="#api-vs-abi-concretely" aria-label="Permalink to &quot;API vs ABI, concretely&quot;">​</a></h2><p>Consider a C function:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}"> a</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}"> b</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span></code></pre></div><ul><li><strong>API:</strong> the signature <code>int add(int, int)</code> and the header <code>add.h</code>. If a new version renames a parameter or keeps the same types, the API is unchanged and old code recompiles fine.</li><li><strong>ABI:</strong> also specifies that <code>a</code> and <code>b</code> go in registers (e.g., <code>rdi</code>, <code>rsi</code> on x86-64 System V), the result comes back in <code>rax</code>, the symbol is literally <code>add</code> (not C++-mangled), and <code>int</code> is 32 bits. If any of these change, an already-compiled <code>.so</code> or executable breaks.</li></ul><h3 id="a-breaking-abi-change-that-does-not-break-the-api" tabindex="-1">A breaking ABI change that does not break the API <a class="header-anchor" href="#a-breaking-abi-change-that-does-not-break-the-api" aria-label="Permalink to &quot;A breaking ABI change that does not break the API&quot;">​</a></h3><p>Reordering <code>struct</code> fields changes the binary offsets:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">struct</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> S { </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">char</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> c; </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> i; };</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   // size 8 (1 + 3 pad + 4)</span></span></code></pre></div><p>→</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">struct</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> S { </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> i; </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">char</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> c; };</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   // size 5 (different layout)</span></span></code></pre></div><p>The type and field <em>names</em> are the same API, but compiled code that reads field <code>c</code> at offset 4 now reads garbage. The ABI broke; the API didn&#39;t.</p><h2 id="system-calls-the-kernel-entry-point" tabindex="-1">System calls: the kernel entry point <a class="header-anchor" href="#system-calls-the-kernel-entry-point" aria-label="Permalink to &quot;System calls: the kernel entry point&quot;">​</a></h2><p>A <strong>system call</strong> is the program&#39;s request for OS service. The path:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>program sets args in registers → issue \`syscall\` instruction →</span></span>
<span class="line"><span>CPU switches to kernel mode → kernel dispatches handler →</span></span>
<span class="line"><span>kernel returns result → program resumes in user mode</span></span></code></pre></div><h3 id="example-x86-64-linux" tabindex="-1">Example (x86-64 Linux) <a class="header-anchor" href="#example-x86-64-linux" aria-label="Permalink to &quot;Example (x86-64 Linux)&quot;">​</a></h3><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">#include</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &lt;unistd.h&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">long</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> _start</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    const</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> char</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> msg</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">[]</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;hi</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">\\n</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    syscall</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(SYS_write, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, msg, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   // write(fd=1, buf=msg, count=2)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    syscall</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(SYS_exit, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>At the ABI level this means: <code>rax = 1</code> (write), <code>rdi = fd</code>, <code>rsi = buf</code>, <code>rdx = count</code>; the kernel returns the byte count in <code>rax</code>. The exact register assignment <strong>is the ABI</strong>.</p><h2 id="protected-mode-and-drivers" tabindex="-1">Protected mode and drivers <a class="header-anchor" href="#protected-mode-and-drivers" aria-label="Permalink to &quot;Protected mode and drivers&quot;">​</a></h2><p><strong>Protected mode</strong> (x86) / <strong>hypervisor mode</strong> (ARM EL1) is the hardware support that makes user/kernel separation real:</p><ul><li>Virtual addresses are translated (MMU) and <strong>checked</strong> against the process&#39;s page tables and ring 2/3 permissions.</li><li>I/O instructions (<code>in</code>/<code>out</code>, accessing PCIe MMIO) are <strong>privileged</strong>; only ring 0 can execute them. A user <code>in</code> instruction traps to the kernel.</li><li>The OS runs in <strong>ring 0</strong>; applications run in <strong>ring 3</strong>. Drivers are kernel modules — which is exactly why a bad driver can crash the whole system.</li></ul><h2 id="keeping-binaries-compatible" tabindex="-1">Keeping binaries compatible <a class="header-anchor" href="#keeping-binaries-compatible" aria-label="Permalink to &quot;Keeping binaries compatible&quot;">​</a></h2><p>Operating systems keep old programs working through:</p><ul><li><strong>Symbol versioning</strong> (glibc attaches <code>GLIBC_2.2.5</code>, <code>GLIBC_2.34</code>, … to each symbol so old and new callers link to the right version).</li><li><strong>Deprecation without removal</strong>: keep old symbols forever; add new ones.</li><li><strong>Stable ABIs at the boundary</strong>: POSIX APIs and the Linux syscall ABI are kept stable even as kernels evolve.</li></ul><h2 id="startup-scripts-and-bootstrapping" tabindex="-1">Startup scripts and bootstrapping <a class="header-anchor" href="#startup-scripts-and-bootstrapping" aria-label="Permalink to &quot;Startup scripts and bootstrapping&quot;">​</a></h2><h3 id="boot" tabindex="-1">Boot <a class="header-anchor" href="#boot" aria-label="Permalink to &quot;Boot&quot;">​</a></h3><ol><li><strong>Firmware (UEFI/BIOS):</strong> runs POST, initializes hardware, selects a boot device.</li><li><strong>Bootloader (GRUB/systemd-boot):</strong> reads its config, loads the <strong>kernel</strong> image and an <strong>initrd</strong> (initial RAM disk with early drivers).</li><li><strong>Kernel:</strong> mounts <code>rootfs</code>, brings up memory management and devices, starts the first process, <strong>init</strong> (PID 1, or <code>systemd</code>).</li><li><strong>init/systemd:</strong> bring up services in dependency order — this is what &quot;startup scripts&quot; used to mean (SysV <code>/etc/init.d/</code> scripts), now <strong>systemd units</strong> started in parallel.</li></ol><h3 id="startup-scripts-today" tabindex="-1">Startup scripts today <a class="header-anchor" href="#startup-scripts-today" aria-label="Permalink to &quot;Startup scripts today&quot;">​</a></h3><ul><li><code>systemd</code> unit files replace <code>/etc/init.d/*</code> scripts.</li><li><code>systemctl enable &lt;name&gt;</code> marks a service to start at boot; <code>systemctl start</code> runs it now.</li><li>The equivalent of &quot;run level 3&quot; is a target like <code>multi-user.target</code>; &quot;run level 5&quot; is <code>graphical.target</code>.</li></ul><h2 id="exam-angle" tabindex="-1">Exam angle <a class="header-anchor" href="#exam-angle" aria-label="Permalink to &quot;Exam angle&quot;">​</a></h2><p>For &quot;API vs ABI&quot;:</p><ol><li>Define both; stress that API = source-level, ABI = binary-level.</li><li>Give a concrete difference: changing struct layout breaks ABI, not API.</li><li>For system calls: trace the register setup + <code>syscall</code> + kernel-mode switch.</li><li>Mention protected mode gives ring 0 to the kernel (and drivers), ring 3 to applications.</li></ol><blockquote><p>Tip: when comparing &quot;API change that breaks ABI,&quot; <strong>struct field reordering</strong> is the textbook example examiners expect.</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Articles/Module-10-Article.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module10Article = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module10Article as default
};
