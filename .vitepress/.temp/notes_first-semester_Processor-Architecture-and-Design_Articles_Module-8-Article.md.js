import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 8 Article: Virtual Memory Under the Hood","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-8-Article.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-8-Article.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Articles/Module-8-Article.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-8-article-virtual-memory-under-the-hood" tabindex="-1">Module 8 Article: Virtual Memory Under the Hood <a class="header-anchor" href="#module-8-article-virtual-memory-under-the-hood" aria-label="Permalink to &quot;Module 8 Article: Virtual Memory Under the Hood&quot;">​</a></h1><h2 id="the-big-idea" tabindex="-1">The big idea <a class="header-anchor" href="#the-big-idea" aria-label="Permalink to &quot;The big idea&quot;">​</a></h2><p>Virtual memory makes every program believe it has a <strong>big, private, contiguous memory</strong>, even when physical RAM is small or shared. It does this by giving each process a <strong>virtual address space</strong> and translating addresses on the fly, using the disk as overflow when RAM fills up.</p><h2 id="paging-chopping-memory-into-pages" tabindex="-1">Paging: chopping memory into pages <a class="header-anchor" href="#paging-chopping-memory-into-pages" aria-label="Permalink to &quot;Paging: chopping memory into pages&quot;">​</a></h2><p>Physical RAM and each process&#39;s virtual space are both divided into equal-sized <strong>pages</strong> (commonly 4 KiB). A 32-bit address splits as:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Bits 31..12 : Virtual Page Number (VPN)   20 bits → 1,048,576 pages</span></span>
<span class="line"><span>Bits 11..0  : Page offset                  12 bits → 4096 bytes</span></span></code></pre></div><p>The <strong>page offset never changes</strong> during translation; only the page number is mapped. The OS keeps a <strong>page table</strong> per process: VPN → PFN (Physical Frame Number) plus permission/flag bits.</p><h2 id="page-fault-the-slow-path-that-loads-a-page" tabindex="-1">Page fault: the slow path that loads a page <a class="header-anchor" href="#page-fault-the-slow-path-that-loads-a-page" aria-label="Permalink to &quot;Page fault: the slow path that loads a page&quot;">​</a></h2><ol><li>Program accesses virtual page <code>V</code>; the page table says &quot;not present.&quot;</li><li>The MMU raises a <strong>page fault</strong> exception.</li><li>The OS handler: <ul><li>Finds a free physical frame (or evicts a victim, writing it back if dirty).</li><li>Schedules a disk read to load the page.</li><li>Updates the page table: <code>V → frame</code>.</li><li>Returns to the faulting instruction.</li></ul></li><li>The instruction <strong>restarts</strong> and now hits in the TLB.</li></ol><h2 id="required-hardware-mmu-and-tlb" tabindex="-1">Required hardware: MMU and TLB <a class="header-anchor" href="#required-hardware-mmu-and-tlb" aria-label="Permalink to &quot;Required hardware: MMU and TLB&quot;">​</a></h2><h3 id="memory-management-unit-mmu" tabindex="-1">Memory Management Unit (MMU) <a class="header-anchor" href="#memory-management-unit-mmu" aria-label="Permalink to &quot;Memory Management Unit (MMU)&quot;">​</a></h3><p>Every memory access goes through the MMU, which translates the virtual address to a physical address. To do this translation it consults the <strong>page table in memory</strong> — but that would double every memory access. So:</p><h3 id="translation-lookaside-buffer-tlb" tabindex="-1">Translation Lookaside Buffer (TLB) <a class="header-anchor" href="#translation-lookaside-buffer-tlb" aria-label="Permalink to &quot;Translation Lookaside Buffer (TLB)&quot;">​</a></h3><p>A small, fully-associative <strong>cache of recent translations</strong> sits next to the MMU. On a virtual address:</p><ul><li><strong>TLB hit (≈ 1–2 cycles):</strong> translation is immediate.</li><li><strong>TLB miss:</strong> the MMU walks the page-table tree in memory (multiple accesses), then caches the result in the TLB.</li></ul><p>The TLB is the reason &quot;thrashing&quot; the page tables makes a program very slow.</p><h3 id="protection-bits-per-page-table-entry" tabindex="-1">Protection bits per page table entry <a class="header-anchor" href="#protection-bits-per-page-table-entry" aria-label="Permalink to &quot;Protection bits per page table entry&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Bit</th><th>Name</th><th>Meaning</th></tr></thead><tbody><tr><td>P</td><td>Present</td><td>Page is in RAM (1) or on disk (0)</td></tr><tr><td>R/W</td><td>Read/Write</td><td>Read-only page vs writable</td></tr><tr><td>X</td><td>Execute-Disable</td><td>No instruction fetch from this page</td></tr><tr><td>U/S</td><td>User/Supervisor</td><td>User mode can (1) or cannot (0) access</td></tr><tr><td>D</td><td>Dirty</td><td>Page has been written (write it back when evicted)</td></tr><tr><td>A</td><td>Accessed</td><td>Page has been read/written (used for replacement)</td></tr></tbody></table><h2 id="demand-paging-and-replacement" tabindex="-1">Demand paging and replacement <a class="header-anchor" href="#demand-paging-and-replacement" aria-label="Permalink to &quot;Demand paging and replacement&quot;">​</a></h2><ul><li><strong>Demand paging:</strong> load a page <strong>only when first accessed</strong>, not in advance. This is why a program can be larger than physical memory.</li><li><strong>Replacement:</strong> when RAM is full, evict a victim frame. The classic goal is <strong>LRU (Least Recently Used)</strong>; true LRU is expensive, so the OS approximates it using the <strong>accessed</strong> and <strong>dirty</strong> bits (the clock/second-chance algorithm).</li><li><strong>Copy-on-write (COW):</strong> after <code>fork()</code>, parent and child share the same physical pages, mapped read-only. Only when one side writes does the kernel copy the page. This makes <code>fork</code>+<code>exec</code> cheap.</li></ul><h2 id="shared-memory-and-memory-mapped-files" tabindex="-1">Shared memory and memory-mapped files <a class="header-anchor" href="#shared-memory-and-memory-mapped-files" aria-label="Permalink to &quot;Shared memory and memory-mapped files&quot;">​</a></h2><ul><li><strong>Shared memory:</strong> map the <strong>same physical frame</strong> into two processes&#39; address spaces (e.g., <code>shmget</code>/<code>mmap(MAP_SHARED)</code>). Fast IPC, but needs explicit synchronization (mutexes/semaphores).</li><li><strong>Memory-mapped files:</strong> map a file (or device) into the address space so reading memory touches the file&#39;s backing pages, loaded on demand. The file is not a special case of I/O — it looks like ordinary memory.</li></ul><h2 id="worked-example-a-page-table-walk" tabindex="-1">Worked example: a page-table walk <a class="header-anchor" href="#worked-example-a-page-table-walk" aria-label="Permalink to &quot;Worked example: a page-table walk&quot;">​</a></h2><p>Process accesses virtual <code>0x00403004</code>. VPN = <code>0x00403</code>, offset = <code>0x004</code>.</p><ul><li>The hardware/page-table walker follows the page table (or, on x86-64, a 4-level tree: PML4 → PDPT → PD → PT).</li><li>It finds <code>VPN 0x00403 → PFN 0x1A200</code>, with bits <code>P=1, R/W=1, U/S=1, D=0, A=1</code>.</li><li>Physical address = <code>(0x1A200 &lt;&lt; 12) | 0x004 = 0x1A200004</code>.</li><li>The translation is cached in the TLB for the next access.</li></ul><h2 id="exam-angle" tabindex="-1">Exam angle <a class="header-anchor" href="#exam-angle" aria-label="Permalink to &quot;Exam angle&quot;">​</a></h2><p>For a virtual-memory question:</p><ol><li>Define <strong>virtual address space</strong> and why the offset is preserved.</li><li>Describe the <strong>page fault</strong> flow: fault → OS selects a victim → swap in → update page table → restart.</li><li>Explain the <strong>TLB</strong> and why it&#39;s needed (avoiding a page-table walk per access).</li><li>State the meaning of at least three page-table bits (P, R/W, U/S, D, A).</li><li>Mention <strong>copy-on-write</strong> as a use of these bits after <code>fork</code>.</li></ol><blockquote><p>Tip: distinguish a <strong>page fault</strong> (recoverable, demand paging) from a <strong>segmentation fault</strong> (unrecoverable, bad address/violation). Both go through the same exception hardware, but the OS resolves one and kills the program on the other.</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Articles/Module-8-Article.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module8Article = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module8Article as default
};
