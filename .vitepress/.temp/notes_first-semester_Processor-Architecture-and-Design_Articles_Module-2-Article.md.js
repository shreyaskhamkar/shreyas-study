import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Module 2 Article: Building Processors from Gates","description":"","frontmatter":{},"headers":[],"relativePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-2-Article.md","filePath":"notes/first-semester/Processor-Architecture-and-Design/Articles/Module-2-Article.md","lastUpdated":null}');
const _sfc_main = { name: "notes/first-semester/Processor-Architecture-and-Design/Articles/Module-2-Article.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="module-2-article-building-processors-from-gates" tabindex="-1">Module 2 Article: Building Processors from Gates <a class="header-anchor" href="#module-2-article-building-processors-from-gates" aria-label="Permalink to &quot;Module 2 Article: Building Processors from Gates&quot;">​</a></h1><h2 id="the-big-idea" tabindex="-1">The big idea <a class="header-anchor" href="#the-big-idea" aria-label="Permalink to &quot;The big idea&quot;">​</a></h2><p>Every digital computer is built, at the bottom, from <strong>logic gates</strong> that combine 0s and 1s. Combinational gates make decisions &quot;now&quot;; flip-flops add memory so machines can &quot;remember.&quot; Stacking these gives registers, adders, and finally the datapath and control of a real CPU.</p><h2 id="logic-gates-and-combinational-circuits" tabindex="-1">Logic gates and combinational circuits <a class="header-anchor" href="#logic-gates-and-combinational-circuits" aria-label="Permalink to &quot;Logic gates and combinational circuits&quot;">​</a></h2><p>A <strong>bit</strong> is 0 (LOW) or 1 (HIGH). The basic gates:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>NOT : Y = ¬A</span></span>
<span class="line"><span>AND : Y = A·B</span></span>
<span class="line"><span>OR  : Y = A + B</span></span>
<span class="line"><span>XOR : Y = A ⊕ B</span></span>
<span class="line"><span>NAND: Y = ¬(A·B)   (universal)</span></span>
<span class="line"><span>NOR : Y = ¬(A + B)  (universal)</span></span></code></pre></div><p>Because NAND and NOR are <strong>universal</strong>, you can build any Boolean function using only one of them. A <strong>combinational</strong> circuit&#39;s output depends <strong>only</strong> on current inputs — think of it as a pure function of the inputs.</p><h3 id="building-an-adder" tabindex="-1">Building an adder <a class="header-anchor" href="#building-an-adder" aria-label="Permalink to &quot;Building an adder&quot;">​</a></h3><p>A <strong>half-adder</strong> adds two 1-bit numbers:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Sum   = A ⊕ B</span></span>
<span class="line"><span>Carry = A · B</span></span></code></pre></div><p>A <strong>full-adder</strong> takes a carry-in, letting us chain them for multi-bit addition:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>S  = A ⊕ B ⊕ Cin</span></span>
<span class="line"><span>Cout = (A·B) + (Cin·(A ⊕ B))</span></span></code></pre></div><p>Four full adders chained = a 4-bit adder. This same structure appears inside the ALU that every CPU uses.</p><h2 id="sequential-logic-and-the-d-flip-flop" tabindex="-1">Sequential logic and the D flip-flop <a class="header-anchor" href="#sequential-logic-and-the-d-flip-flop" aria-label="Permalink to &quot;Sequential logic and the D flip-flop&quot;">​</a></h2><p>The key missing piece from combinational logic is <strong>memory</strong>: a way to hold a value. The <strong>D flip-flop</strong> is the workhorse:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>On the rising clock edge:  Q ← D</span></span>
<span class="line"><span>Otherwise: Q holds its last value</span></span></code></pre></div><p>Two rules matter:</p><ul><li><strong>Setup time:</strong> D must be stable <em>before</em> the clock edge.</li><li><strong>Hold time:</strong> D must stay stable <em>after</em> the clock edge.</li></ul><p>Violating either → <strong>metastability</strong> (the output is unpredictable). A <strong>register</strong> is several flip-flops sharing a clock; a <strong>register file</strong> is a bank of registers you can address by number.</p><h2 id="from-gates-to-a-finite-state-machine" tabindex="-1">From gates to a finite-state machine <a class="header-anchor" href="#from-gates-to-a-finite-state-machine" aria-label="Permalink to &quot;From gates to a finite-state machine&quot;">​</a></h2><p>A <strong>finite-state machine (FSM)</strong> is the bridge from gates to control logic:</p><ul><li><strong>State:</strong> the current memory (stored in flip-flops).</li><li><strong>Input:</strong> observed now.</li><li><strong>Output:</strong> produced now.</li><li><strong>Next-state logic:</strong> combinational function of (current state, input).</li></ul><p>Example: a light controller with states <code>OFF → ON → BLINK → OFF</code>. The state flip-flops remember where we are; combinational logic decides, for each input, the next state and the lights&#39; outputs.</p><h3 id="the-counter-worked-example" tabindex="-1">The counter worked example <a class="header-anchor" href="#the-counter-worked-example" aria-label="Permalink to &quot;The counter worked example&quot;">​</a></h3><p>A 2-bit counter cycles <code>00 → 01 → 10 → 11 → 00</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Present (Q1 Q0)  Next (Q1* Q0*)</span></span>
<span class="line"><span>00              01      (D1=0, D0=1)</span></span>
<span class="line"><span>01              10      (D1=1, D0=0)</span></span>
<span class="line"><span>10              11      (D1=1, D0=1)</span></span>
<span class="line"><span>11              00      (D1=0, D0=0)</span></span></code></pre></div><p>Truth-table → Karnaugh map → equations:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>D0 = ¬Q0        (toggle every step)</span></span>
<span class="line"><span>D1 = Q1 ⊕ Q0    (toggle every other step)</span></span></code></pre></div><h2 id="building-blocks-of-a-datapath" tabindex="-1">Building blocks of a datapath <a class="header-anchor" href="#building-blocks-of-a-datapath" aria-label="Permalink to &quot;Building blocks of a datapath&quot;">​</a></h2><p>A CPU <strong>datapath</strong> is just an organized network of these building blocks:</p><ul><li><strong>Register file:</strong> 32 registers you can read/write by number.</li><li><strong>ALU:</strong> add, subtract, AND, OR, shift.</li><li><strong>Bus:</strong> shared wires; a <strong>multiplexer</strong> selects which register drives it.</li><li><strong>Control signals:</strong> route operands, pick the ALU op, enable writes.</li></ul><p>The <strong>control unit</strong> (next module) just produces the right pattern of control signals for each instruction. So really, a CPU is: many gates, a few registers, an ALU, and a controller that knows how to connect them.</p><h2 id="exam-angle" tabindex="-1">Exam angle <a class="header-anchor" href="#exam-angle" aria-label="Permalink to &quot;Exam angle&quot;">​</a></h2><p>For a &quot;design a circuit&quot; question:</p><ol><li>State the function in words, then the truth table.</li><li>Derive the minimal Boolean expression (K-map or algebra).</li><li>Draw the gate-level schematic with labels.</li><li>If sequential, specify the flip-flop input equations and the state table.</li></ol><blockquote><p>Tip: when asked about flip-flop timing, always mention <strong>setup</strong> and <strong>hold</strong> time — marks reward the detail even for small questions.</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/first-semester/Processor-Architecture-and-Design/Articles/Module-2-Article.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const Module2Article = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  Module2Article as default
};
